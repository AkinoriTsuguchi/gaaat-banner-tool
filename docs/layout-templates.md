# GAAAT バナー自動生成ツール開発メモ
### レイアウトテンプレート整理(過去バナー5パターン)

このメモは、GAAATの展示会バナーを「会期差し替え」「多言語対応」「作品の世界観に合わせたデザイン選択」まで自動生成できるツールを開発するための土台資料。

---

## 開発方針(前提)

- キャラクター原画・作品画像は **1pxも加工・変更しない**(固定レイヤーとして合成)
- 「世界観に合わせる」は完全な生成AIに背景を自由に作らせるのではなく、**下記5パターンのレイアウトテンプレートから自動選択**する方式で実現する(品質のブレ・監修コストを抑えるため)
- 配色(背景色・アクセントカラー・文字色)は作品画像から自動抽出する
- 会期情報・多言語テキストは差し替え可能な項目として設計する

---

## テンプレート①：キャラクター切り抜き型

**参照元**：キャプテン翼展バナー

| 要素 | 内容 |
|---|---|
| 画像の扱い | 白背景から浮かせる形でキャラクターを右下寄りに大きく斜め配置(切り抜き) |
| タイトル配置 | 左上に企画タイトルの大型ロゴ(グラデーション)、その下に会期・企画名の細字サブタイトル |
| コピー配置 | 左下にキャッチコピー(3行程度)、左端にカラーの縦アクセントバー |
| 装飾 | キャラクター背後にシルエット(躍動感の演出) |
| ロゴ/コピーライト | 右下に固定 |
| 雰囲気 | ポップ・エネルギッシュ |
| 向いている作品 | 少年漫画・スポーツ系 |

---

## テンプレート②：フルブリード背景+縦書きタイトル型

**参照元**：サイボーグ009展バナー

| 要素 | 内容 |
|---|---|
| 画像の扱い | 作品イラストを画面いっぱいに配置(背景に原作コマ割りのコラージュを薄く敷く) |
| タイトル配置 | 右側に展タイトルを縦書き・大型で配置 |
| コピー配置 | 左側に日付・時間を回転テキストで配置、下部に開催場所を横組み |
| 装飾 | 下部に赤いリボン帯でCTA(例：「限定アート展開催！」) |
| ロゴ/コピーライト | ロゴは左上、コピーライトは左下に小さく |
| 雰囲気 | レトロ漫画・ドラマチック |
| 向いている作品 | 和風・古典的な作品 |

---

## テンプレート③：フレーム展示(壁掛け)型

**参照元**：焦茶バナー

| 要素 | 内容 |
|---|---|
| 画像の扱い | 作品を額装して壁に掛けたモックアップとして中央配置(額縁・マット・ガラス反射まで再現) |
| タイトル配置 | 上部中央に見出しコピー1行 |
| コピー配置 | 下部左に作品名ロゴ風テキスト、右にカラー帯でCTA |
| 装飾 | ほぼなし。背景はニュートラルな壁のみ |
| 雰囲気 | ギャラリー的・上品・ミニマル |
| 向いている作品 | 汎用(どのIPにも合わせやすいベース型) |

---

## テンプレート④：スポットライト+額装型

**参照元**：グレンダイザー展バナー(アラビア語版で実装済み)

| 要素 | 内容 |
|---|---|
| 画像の扱い | ③と同じ額装モックアップだが、背景に大きな色付きスポットライト(円形グラデーション)を敷く |
| タイトル配置 | 上部に縁取り+ドロップシャドウの大型タイトル |
| コピー配置 | タイトル下に白文字サブコピー(2行)、下部に色文字でCTA |
| 装飾 | 背景を暗色+差し色の丸spotlightにして世界観を演出(IPカラーに応じて色を変える) |
| ロゴ/コピーライト | ロゴは右上、コピーライトは右下 |
| 雰囲気 | ドラマチック・重厚 |
| 向いている作品 | ヒーロー・特撮・重厚系 |
| 備考 | **アラビア語(右から左書き)での実装例が既にある**。多言語対応の技術検証にそのまま使える |

---

## テンプレート⑤：サイバーUI・フルブリード型

**参照元**：攻殻機動隊展バナー

| 要素 | 内容 |
|---|---|
| 画像の扱い | 作品イラストを画面いっぱいに配置 |
| コピー配置 | 中央〜下部にキャッチコピーを重ねる(例：「最大32層で魅せる『攻殻機動隊』」) |
| 装飾 | 画面各所に疑似システムUIテキスト(「ERROR CODE 000.」「SYSTEM-TT」等)を散りばめる。CTAバッジ(「ONLINE SALE」)あり |
| 雰囲気 | 近未来・テック感 |
| 向いている作品 | SF・近未来系 |

---

## まとめ表

| パターン | 画像の扱い | タイトル配置 | 装飾 | 向いてる作品 |
|---|---|---|---|---|
| ①キャラ切り抜き | 切り抜き大配置 | 左上・横組み大型 | シルエット装飾あり | 少年漫画・スポーツ系 |
| ②縦書きコラージュ | フルブリード+コラージュ | 右側・縦書き大型 | 原稿コラージュ背景 | 和風・レトロ漫画系 |
| ③壁掛けフレーム | 額装モックアップ | 上部センター横1行 | ほぼなし(汎用) | 何でも合う(ベース型) |
| ④スポットライト額装 | 額装+背景演出 | 上部センター縁取り | 色spotlight背景 | ヒーロー・特撮・重厚系 |
| ⑤サイバーUI | フルブリード | 中央〜下オーバーレイ | 疑似UIラベル散在 | SF・近未来系 |

---

## 世界観の自動選択ロジック(たたき台)

作品画像から抽出した配色・トーンをもとに、以下のようなルールでテンプレートを振り分ける想定。

- 暖色系・彩度高め・ポップ → テンプレート①
- モノトーン・和紙質感・墨系 → テンプレート②
- 特に指定なし・汎用 → テンプレート③(デフォルト)
- 暗色・単色スポット映え・重厚 → テンプレート④
- 寒色・グレー系・デジタル感 → テンプレート⑤

※最初はルールベースで始めて、運用しながら精度を調整するのが現実的。

---

## 次のステップ(Claude Codeでの実装範囲)

1. 上記5パターンをレイヤー構造のテンプレートファイル(SVGまたは画像合成用データ)として作成
2. キャラクター/作品画像を固定レイヤーとして合成する処理を実装(1px変更禁止)
3. 配色自動抽出ロジックを実装し、テンプレート自動選択ロジックと接続
4. 会期テキスト・多言語テキストの差し替え機能を追加(繁体字・簡体字・アラビア語(RTL)は専用フォント・レイアウト調整が必要)
5. GAAAT案件一覧マスタ(スプレッドシート)と連携し、会期×言語×都市の組み合わせを一括生成できるようにする

---

## 現状の実装メモ(2026-08-31 時点)

- [app.js](../app.js) / [index.html](../index.html) には、レイアウト選択プルダウンで切り替え可能な複数テンプレートが実装済み。
  - 「情報帯型(現行レイアウト)」: 元からあったカスタムレイアウト(テンプレート④相当ではなく独自構成)
  - テンプレート①キャラクター切り抜き型: `renderCutoutTemplate()`
  - テンプレート②縦書きタイトル型: `renderVerticalTitleTemplate()`
  - テンプレート③壁掛けフレーム型: `renderFrameTemplate()`
  - テンプレート④スポットライト額装型: `renderSpotlightFrameTemplate()`(RTL/アラビア語対応の参照実装。③④共通の額装描画は `drawPictureFrame()` に共通化)
  - テンプレート⑤サイバーUI型: `renderCyberUiTemplate()`
- 世界観の自動選択ロジックを実装済み: `classifyTemplateFromPalette()`。原画アップロード時に配色(背景の明るさ・彩度、アクセントの色相と生の彩度)からテンプレートを自動判定し、プルダウンに反映する。UIの「原画の配色からレイアウトを自動選択」チェックボックス(デフォルトON)でON/OFF可能。手動でプルダウンを変更すると自動でOFFになる。
- 実装済み機能: 案件マスタのスプレッドシート貼り付けパース、言語別テキストドラフト管理、原画からの配色自動抽出(背景色・アクセント・文字色コントラスト判定)、1080×1080キャンバスへの描画、PNGダウンロード、**都市×言語の一括生成(ZIP一括ダウンロード)**。

### 一括生成機能(2026-08-31 追加)

- 案件マスタを貼り付けると、複数都市がある場合はチェックリストが表示される(デフォルト全選択)。「言語」欄に追加済みの全言語 × チェックした全都市の組み合わせで自動生成し、ZIPでダウンロードする(`runBatchGenerate()` in [app.js](../app.js))。
- 都市×言語ごとに、会場名・会期日程（都市側）とタイトル/メインコピー/サブコピー（言語側）だけを差し替え、レイアウト・原画・配色は固定のまま描画する。
- ZIP化には [JSZip](https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js) をCDNから読み込んで使用(オフライン等でJSZipが読み込めない場合は、個別PNGを順次ダウンロードするフォールバックあり)。
- ファイル名は `{都市}_{言語コード}.png`(重複時は連番を付与)。

## バナー用途(集客用 / オンライン販売用)(2026-08-31 追加)

- 新しい「バナー用途」セレクト(`els.bannerPurposeSelect`, `state.bannerPurpose`: `'venue'` | `'sale'`)を追加。データモデルを増やしすぎないよう、既存の会場名・会期フィールドを**そのまま流用して意味を切り替える**設計にした。
  - `sale`選択時、会場名欄のラベルが「販売サイト名」、会期欄が「販売期間(任意)」、サブコピー欄が「CTA文言」に変わる(`applyBannerPurposeUI()`)。中身のフィールド・データ構造は変えていない。
  - 新規フィールドは「価格・オファー文言」(`els.priceField`)ひとつだけ追加。`sale`モードの時だけ表示され、非空なら各テンプレートの会場/会期ラインに追記される。
  - 会場・会期・価格をまとめる処理は `buildInfoLineParts()` に共通化し、③④⑤①の情報ラインで共有(②と現行レイアウトは独自レイアウトのため会場テキストに価格を連結する形で個別対応)。
  - ③④②のCTAピル/バッジ/帯は、ユーザーが本文を何も入力していない場合に限り `saleCtaFallback()`(「ONLINE STORE」)にフォールバックする。ユーザー入力があれば常にそちらが優先される。
- 集客用(デフォルト)では表示・挙動は追加前と完全に同一(回帰確認済み)。

## Googleドライブ連携(2026-08-31 実装、要セットアップ)

「キャラクター原画」欄の下に、Googleドライブのフォルダから直接画像を選んで読み込める機能を追加した(`app.js` の「Google Drive import」セクション、`index.html` の `.drive-section`)。

- Google Identity Services (GIS) のトークンフローでOAuthログインし、Drive API v3のREST APIを直接fetchで叩く実装(gapiクライアントライブラリ不要)。
- フォルダURL(`.../drive/folders/{id}` など)または生のフォルダIDを貼り付けると、フォルダ内の画像ファイル一覧をサムネイル付きグリッドで表示。クリックすると通常のファイルアップロードと全く同じ処理(配色自動抽出→レイアウト自動選択→描画)が走る。
- 案件マスタを貼り付け済みの場合、ファイル名にPJコードまたは開催都市名が含まれるファイルを「候補」バッジ付きでハイライトする(`matchesParsedSession()`)。これは正式なPJコード⇔ファイルの紐付けではなく、あくまで「それらしい名前のファイルを目立たせる」ヒューリスティック。

### スプレッドシートURLからの自動取得(2026-08-31 追加)

「案件マスタから貼り付け」欄に、コピペ範囲の代わりにスプレッドシートのURL(該当タブを開いた状態、`#gid=...`付き)を貼るだけで自動取得できるようにした。

- Driveと同じGoogleログイン(同じOAuthトークン)で、追加スコープ `spreadsheets.readonly` を使ってSheets API v4を直接fetchで叩く。
- URLから`spreadsheetId`と`gid`を抽出 → `spreadsheets.get`でgid→タブ名を解決 → `spreadsheets.values.get`でそのタブのセル値を取得 → 取得した2次元配列をTSV文字列に変換して、**手動貼り付けと全く同じパーサー**(`applyParsedMasterText()`)に渡す、という流れ。パース・反映ロジックは完全に共通化されているので、手動貼り付けと自動取得で結果がズレることはない。
- 未ログインでURL取得ボタンを押すと案内メッセージを出す。ログイン後は「キャラクター原画」欄のGoogleログインと共有される。

### ⚠️ 動作させるには初期セットアップが必要(ユーザー側の作業)

Claude Codeからはユーザー自身のGoogle Cloudアカウントを操作できないため、OAuthクライアントIDの発行だけはGAAAT側で行う必要がある。手順:

1. [Google Cloud Console](https://console.cloud.google.com/) で新規プロジェクトを作成(または既存のものを利用)。
2. 「APIとサービス」→「ライブラリ」から **Google Drive API** と **Google Sheets API** の両方を有効化(案件マスタの自動取得にはSheets APIも必要)。
3. 「APIとサービス」→「OAuth同意画面」を設定(社内利用なら「内部」、外部共有もするなら「外部」を選択してテストユーザーを追加)。
4. 「APIとサービス」→「認証情報」→「認証情報を作成」→「OAuthクライアントID」→ アプリケーションの種類は **ウェブ アプリケーション** を選択。
5. 「承認済みのJavaScript生成元」に、このツールを開くURLのオリジンを追加(例: ローカルで動かすなら `http://localhost:8934`、社内サーバーにホストするならそのドメイン)。
6. 発行された **クライアントID**(`xxxxx.apps.googleusercontent.com` の形式)をコピーし、[app.js](../app.js) 内の

   ```js
   const GOOGLE_CLIENT_ID = 'YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com';
   ```

   の値を置き換える。

設定が済むまでは「Googleでログイン」ボタンは無効化され、上に設定手順の案内文が表示される(未設定でもツール全体が壊れることはない)。設定後は、フォルダを読み込む前提としてそのフォルダがログインするGoogleアカウントと共有されている必要がある。

## レイアウト微調整パネル(2026-08-31 実装)

**背景**: 監修(版元)からのフィードバックには、①テキスト・日付・会場・配色などツールの入力項目で直せるものと、②要素の位置・サイズ・余白などレイアウト自体の調整の2種類がある。①は既存の入力欄を直して一括生成すれば済むが、②はこれまでテンプレートのコード自体を書き換えるしかなく、しかも「版元ごとに指摘箇所が違う」ため、ある案件向けにテンプレートを直すと別の案件に悪影響が出る恐れがあった。かといってPSDで都市×言語ぶん手直しするのは非常に手間、というのが元々の相談。

**対応**: テンプレートのコードは一切変えず、**その案件のセッション内だけで効く上書きレイヤー**として`state.adjustments`を追加した。

- 対象は版元指摘で特に多いという3カテゴリ×6項目: ロゴ・コピーライト・タイトル・メインコピー・サブコピー・原画(トリミング/位置)。各項目に「サイズ%(100=元のまま)」「横位置dx(px)」「縦位置dy(px)」の3つの数値入力(新設の「レイアウト微調整」フィールドセット、18個のnumber input)。
- `adj(key)`という安全アクセサ経由で、6テンプレート全ての該当描画箇所(ロゴ5箇所・コピーライト6箇所・タイトル6箇所・メインコピー5箇所・サブコピー5箇所・原画5箇所、計32箇所)に適用。フォントサイズには`* scale/100`を、描画座標には`+ dx`/`+ dy`を掛け合わせる形で、既存のレイアウト計算にオーバーレイする。
- **原画のトリミング/位置**は少し特殊で、`computeCoverRect()`/`drawCoverImageTo()`/`drawCoverImage()`に`zoom`(scale/100を渡す)と`panX`/`panY`(dx/dyをそのまま渡す)を追加。`zoom>1`で原画を中心から拡大トリミング、`panX`/`panY`で同じ枠内で見せる範囲を平行移動(実際に1px単位でズレないことをピクセル差分で検証済み)。①(キャラクター切り抜き型)の回転した原画パネルだけは`translate`前にdx/dyを加算する形で位置調整し、`drawCoverImageTo`にはzoomだけを渡している。
- `state.adjustments`はプロジェクト(セッション)単位で共有され、言語やあ都市を切り替えても保持される。`runBatchGenerate()`は一切この値に触れないため、一括生成の全都市×全言語に自動的に同じ調整が反映される。ページ再読み込みで消える点は他の入力項目と同じ(意図的、案件ごとの一時的な上書きという性質のため永続化していない)。
- 「この案件の調整をリセット」ボタンで全項目を一括で初期値(100/0/0)に戻せる。
- タイトルなど自動フィット(`fitFontSizeTruncate`)後のサイズにscaleを掛ける箇所は、scaleを100%超に上げすぎると再びはみ出す可能性がある(自動サイズ調整とは違い、目視確認しながら手動で追い込む前提の機能として許容)。

**今後の拡張候補**(ユーザーへの確認済み: 将来的にはドラッグ操作も欲しいとのこと): 上記の数値入力に続き、キャンバス上でのドラッグ/ホイール操作は下記「レイアウト微調整のキャンバス直接操作」で対応済み。

## レイアウト微調整のキャンバス直接操作(2026-08-31 実装)

**背景**: 上記の数値入力パネルを実装した際、「まずは数値入力から。将来的にキャンバス上でドラッグできるようにする拡張余地は残してある」という前提で設計していた。その直後にユーザーから「対応して」と明示的な依頼があり、同日中にドラッグ/ホイール操作を実装した。

**対応**: 数値入力(`state.adjustments`)とキャンバス操作を同じ状態に対して行う、双方向連動の仕組みとして実装。

- 6テンプレートそれぞれの調整可能要素(ロゴ・コピーライト・タイトル・メインコピー・サブコピー・原画、計36箇所)の描画直後に`recordBounds(key, x, y, w, h)`を呼び、「その要素が実際にネイティブ1080px座標のどこに描かれたか」を`state.elementBounds`に記録する。`render()`の先頭で毎回リセットしてから各テンプレート関数を呼ぶので、言語・文字数・現在のadjustments値によって位置やサイズが変わっても常に実際の描画結果と一致する。
- キャンバスの`mousemove`でその点をヒットテスト(`hitTest()`、後から描画された=画面上で手前の要素を優先)し、該当要素があればカーソルを`move`にし、青い破線のハイライト枠(`#adjustHighlight`)とラベルを表示。
- `mousedown`でその要素を掴み、`mousemove`中は移動量をそのまま`state.adjustments[key].dx/dy`に加算して`render()`を呼び直す(ハイライト枠は黄色に変化)。`mouseup`で確定。ドラッグ量はキャンバスの表示サイズ(CSS px)とネイティブ1080px座標のスケール差を`getBoundingClientRect()`で補正して計算しているため、ブラウザの表示倍率に関わらず1:1でずれなく動く。
- 要素の上でホイール操作すると`state.adjustments[key].scale`が4%刻みで増減する(上スクロール=拡大、下スクロール=縮小、10〜400%にクランプ)。
- ドラッグ/ホイールで変わった値は、対応する数値入力(`adjLogoDx`など)にも`syncAdjustmentInputs()`でその場で反映されるため、「キャンバスでだいたいの位置を決めてから数値入力で微調整する」という使い方もできる。逆方向(数値入力→キャンバス)は既存の`render()`呼び出しでそのまま反映される。
- 回転を伴う要素(①キャラクター切り抜き型の斜め原画パネル、②縦書きタイトル型の回転タイトル文字列)は、当たり判定・ハイライト枠を「軸に平行な矩形での近似」にしている(回転後の正確な四角形ではない)。ドラッグの移動量自体は常に非回転の親座標系(dx/dyがそのまま効く座標系)で計算されるため、この近似は当たり判定・見た目のハイライト枠のズレに留まり、実際のドラッグ操作の精度には影響しない(実機で±1px程度の誤差でdx/dyが反映されることを確認済み)。
- ⑤サイバーUI型のサブコピー(CTAバッジ)は、この実装以前は数値入力パネルの対象カテゴリに含まれていながら実際には描画コードにdx/dy/scaleが結線されておらず、ドラッグしても見た目が動かない状態だった(数値入力パネル実装時に「マイナー、スキップ」と記録されていた既知の抜け)。ドラッグ機能を通す過程でこの結線漏れも合わせて修正した。

**動作確認**: `canvas`に`clientX/clientY`付きの`MouseEvent('mousemove')`を直接dispatchしてハイライト表示・ホバー判定を確認した上で、ブラウザ操作ツールでの実際のドラッグ(ロゴのdx/dy、①の回転原画パネルのdx/dy、②の回転タイトルのdx/dy)とホイール操作(ロゴのscale)がいずれも狙った量だけ`state.adjustments`に反映されることを確認済み。6テンプレート全てで`render()`がエラーなく完走し、全調整可能要素の`recordBounds`が正しいキーで登録されることも確認済み。

## レイアウト微調整のUndo/Redo(2026-08-31 実装)

**背景**: ドラッグ操作の実装直後、ユーザーから「これから着手してほしい」機能として明示的に要望があった。ドラッグで意図せず要素を動かしてしまっても戻せないのはストレスになる、という運用上の懸念に対応。

**対応**: `state.adjustments`オブジェクト全体をスナップショットする方式のUndo/Redoスタック(`undoStack`/`redoStack`)を追加。個別フィールドの差分ではなく丸ごとクローンする方式にしたのは、6カテゴリ×3項目程度なら複製コストが無視できるのと、「リセット」のように複数カテゴリを同時に変える操作でも1回のUndoで確実に元へ戻せるため。

- ドラッグ中の連続する`mousemove`、ホイールの連続ティック、数値入力欄への1文字ごとの`input`イベントなど、細かい変更が大量に発生する操作は、「ユーザーの体感としての1アクション」ごとに1つのUndoステップへまとめる(コアレシング)。`recordAdjustmentChange()`を変更のたびに呼ぶが、実際にスナップショットを取るのは連続した変更列の最初の1回だけで、以降は500ms(`UNDO_COALESCE_MS`)のデバウンスタイマーをリセットし続ける。
- 「ドラッグのmouseup」「リセットボタン」のように操作の終わりが明確なものは、そのタイミングで`commitAdjustmentUndoGroup()`を呼んで即座にグループを確定する(デバウンスタイマーの満了を待たない)。ホイールや数値入力のように終わりが明確でない操作は、タイマー満了(操作が止まって500ms経過)で自動確定する。
- Undoボタン・Redoボタン(数値パネル内、リセットボタンの隣)に加えて、`Ctrl+Z`(Windows)/`Cmd+Z`(Mac)でUndo、`Ctrl+Shift+Z`または`Ctrl+Y`でRedoに対応。ただしタイトル・会場名・キャッチコピーなどのテキスト入力欄にフォーカスがある間は、ブラウザ標準のテキストUndoを妨げないようこのショートカットを無効化している(`isTextEditingContext()`で判定)。
- Undo/Redoで戻した値は、対応する数値入力欄(`adjLogoDx`など)にも`syncAdjustmentInputs()`で即座に反映される。新しい変更が発生すると(次のドラッグ・入力など)、Redo履歴は破棄される(一般的なUndo/Redoの挙動と同様)。
- スタックの上限は100件(`UNDO_MAX`)。長時間の作業でも際限なく履歴が積み上がらないようにする安全弁で、実運用でこの上限に達することは想定していない。

**動作確認**: 数値入力→Undo→Redo、5回分の`mousemove`イベントを束ねた擬似ドラッグ(1回のUndoステップにまとまることを確認)→Undo、リセットボタン→Undo(リセット前の値に戻ることを確認)、テキスト欄にフォーカスした状態での`Ctrl+Z`が`state.adjustments`に一切影響しないことを、それぞれ`state.adjustments`と`undoStack`/`redoStack`の中身を直接読み取って確認済み。

## 自動翻訳(2026-08-31 実装、セットアップ不要)

前段の「未翻訳フィールドの警告」だけでは、警告ダイアログを読まずに素通りされて全言語が未翻訳のまま書き出される事象が実際に再発した。ユーザーへの確認の結果、手入力方式から自動翻訳方式に転換することになった。当初はGoogle Cloud Translation APIで実装したが、有効化に請求先アカウント(課金)登録が必須と分かり、「課金なしで自動翻訳したい」という要望を受けて **MyMemory**(https://mymemory.translated.net/doc/spec.php)ベースに切り替えた。

- MyMemoryはサインアップ・APIキー・課金設定が一切不要の無料翻訳API。`fetch`でGETするだけ(CORS対応済み、SDK不要)。無料枠は匿名利用で1日あたり単語数5,000語程度(このツールの用途では通常十分)。
- 「言語」欄に、`scanTranslationGaps()`が未翻訳と判定した項目がある間だけ「未翻訳の項目を自動翻訳」ボタンが表示される。押すと、各言語グループ内で日本語があれば日本語を、なければ設定順で最初の言語を「原文」とみなし、そこから他の言語へ機械翻訳して各言語のドラフトに書き込む(`autoTranslateUntranslated()`)。
- MyMemoryの無料エンドポイントは1リクエスト1文字列のみ(バッチ非対応)なので、タイトルなど複数行のフィールドは改行で分割し、行ごとに順番にAPIを呼び出して(`translateLines()`)結果を元の行位置に戻す。リクエスト間に150msの間隔を空け、無料の共用サービスへの配慮としている。
- 中国語だけは`zh-Hans`/`zh-Hant`という内部コードを`zh-CN`/`zh-TW`に変換する必要があり、`translateLangCode()`で対応。
- 機械翻訳なので、翻訳後も内容の確認・手直しは人間が行う前提(完了メッセージにもその旨を明記)。翻訳後は該当言語チップの「！」バッジが自動的に消える。
- **品質の実例(動作確認時)**: 「歴代シリーズを横断する 唯一無二の展示・販売会。」→ "A one-of-a-kind exhibition and sales event across successive series."(自然)。一方で固有名詞「パトレイバー」が音訳で"Patraber"と誤訳される(正しくは"Patlabor")など、造語・固有名詞の誤訳は起こりうる。無料の翻訳エンジンである以上の限界として認識し、「内容を必ず確認してください」の文言をそのまま残している。

セットアップ手順は不要で、初回から動作する。将来的により高品質な有償エンジン(Google Cloud Translation、DeepLなど)に切り替えたくなった場合は、`translateLine()`関数内のfetch呼び出しを差し替えるだけで済むよう、翻訳ロジックはこの1関数に閉じ込めてある。

## レイヤー分け出力(2026-08-31 実装)

「レイヤー分け出力」フィールドセットから、背景・原画・装飾・テキストの4レイヤーに分けて書き出せる。

- **ZIP(レイヤー別PNG)**: 各レイヤーを透過PNGとして、フラット化した完成形と合わせてZIPに(`downloadLayersZip()`)。
- **PSD**: [ag-psd](https://github.com/Agamnentzar/ag-psd)をCDNから遅延読み込みし(初回クリック時のみ、約800KB)、4レイヤーの名前付きPSDとして書き出す(`downloadPsd()`)。Photoshopで開くと、背景・原画・装飾・テキストの各レイヤーを個別に表示/非表示・移動できる。

### 実装方式(技術メモ)

6テンプレートの描画コード(約2500行)を一切書き換えずにレイヤー分離を実現するため、`ctx` をProxyにして「今どのレイヤー用オフスクリーンcanvasに描画するか」を実行時に切り替える方式にした([app.js](../app.js) 冒頭の「Layer system」セクション)。

- 各テンプレート関数の中に、既存の `// ---- ◯◯ ----` という区切りコメントの直後に `useLayer('text')` のような1行を挿し込むだけで、その区切り以降の描画がその名前のレイヤー(オフスクリーンcanvas)に向かうようになる。個々の `fillRect`/`fillText`/`drawImage` 呼び出し自体は一切変更していない。
- `render()` は `createLayers()` (4枚のレイヤーcanvasを新規作成) → 各テンプレート関数 → `compositeLayers()` (背景→原画→装飾→テキストの順で実キャンバスに合成)という流れ。通常のプレビュー表示・PNGダウンロード・一括生成(ZIP)は今まで通りこの合成済みキャンバスを見ているだけなので、影響を受けない。
- **ハマったポイント**: `drawPictureFrame()`(③④共通の額装描画)は、額縁→フィレット→マット→原画、という順に「手前の色で奥の色を塗りつぶす」ことで額装を表現していた。これは単一キャンバスなら正しく動くが、マット(装飾レイヤー)と原画(原画レイヤー)を別キャンバスに分離し、装飾レイヤーを原画レイヤーより上に合成する今の方式では、マットの塗りつぶしが原画を覆い隠してしまう。額縁・フィレット・マットを「原画窓の形に穴を開けたリング」として`evenodd`クリップで塗ることで解決した(ドロップシャドウも同様にリング状に塗って対応)。
- レイヤー分類は9種(2026-08-31 追記: 当初は`text`1本だったが、「テキスト項目」パネルの入力欄ごとに分けたいという要望を受けて分割): `background` / `artwork` / `decoration` / `title`(展示会タイトル) / `dates`(会期情報) / `venue`(開催会場名・価格/オファー文言) / `mainCopy`(メインコピー) / `subCopy`(サブコピー) / `copyright`(コピーライト)。
  - 会場名・会期・価格を1行にまとめて表示している箇所(③④⑤①のinfoライン)は、`drawInfoLine()`で各パートを個別に測定・配置しつつ、パートごとに別レイヤーへ描画するようにした(区切り文字だけは`decoration`)。
  - CTAテキストのようにサブコピー/メインコピーいずれかにフォールバックする箇所は、実際に使われた側のフィールドのレイヤーに描く(両方空ならデフォルトで`subCopy`)。
- PSD書き出しの正しさは、書き出した直後に ag-psd 自身の `readPsd()` で読み戻し、レイヤーを合成した結果と実キャンバスをピクセル比較して検証済み(差分ゼロ)。

### 直った不具合(2026-08-31)

- `LOGO_DATA_URL`(GAAATロゴのbase64埋め込み)が壊れており、全テンプレートでロゴが無言で表示されていなかった。[logo_horizontal_cropped.png](../logo_horizontal_cropped.png) から正しくbase64を再生成して修正。今後ロゴ画像を差し替える場合は、このファイルをbase64化して `LOGO_DATA_URL` を置き換えること。

## デザイン品質向上パス(2026-08-31 実装)

実際に過去出稿されたバナー実物(`(③-3)R211_機動警察ﾊﾟﾄﾚｲﾊﾞｰ_バナー.jpg`、情報帯型/現行レイアウトの実例)とツール出力を直接比較し、ギャップを2点特定して対応。

- **タイトル用の専用フォント追加**: それまで全テンプレート・全テキストが `FONT_STACK`(Helvetica Neue/Arial等の汎用システムフォント)のみで、見出しも本文も同じ書体だったため「作りかけ」感が出ていた。実物バナーの「PATLABOR」が持つ、コンデンス・強いウェイトの専用書体に近づけるため、Google Fontsから **Oswald**(欧文コンデンス・ボールド)+ **Noto Sans JP**(和文ボールド)を`TITLE_FONT_STACK`として追加し、各テンプレートの主見出し1箇所(⑤サイバーUI型のみメインコピー)に適用。`FONT_STACK`は本文・UI要素にそのまま残置。Canvasはwebフォントの読み込み完了を自動で待たないため、初回`render()`後に`document.fonts.load()`で明示的に読み込み、完了(またはネットワーク遮断時のタイムアウト)後に再`render()`する処理を追加。
- **情報帯型(`renderCurrentLayout`)のレイアウト再構成**: 実物バナーをピクセル解析(`PIL`で背景色との差分行を検出)したところ、原画パネルは大見出し1行目の直後、キャンバス上部14%付近から開始しており、サブタイトル/正式名称は原画の左側だけを使う2カラム構成だと判明。従来はタイトル3行ぶん(大見出し+サブタイトル+正式名称)を全幅中央揃えで描いてから原画を配置していたため、原画上に大きな空白ができていた。大見出し1行目の直後に原画パネルの開始位置(`artTop`)を決め、サブタイトル/正式名称は左カラム幅に収めて左揃えで描くよう変更。原画が上部までブリードするようになり、実物によく近づいた。

比較用に使ったテスト画像・スクリーンショットはスクラッチパッドに保存(リポジトリには含めていない)。他5テンプレートも同条件で再レンダリングし、フォント変更によるはみ出し等がないことを目視確認済み。

### 他案件の実バナーを参照した追加ブラッシュアップ(2026-08-31)

パトレイバー1案件だけでは偏るため、GAAATの共有ドライブ(案件フォルダ一式)から複数案件・複数タイプの実バナーを追加参照した。

- **大暮維人アート展(情報帯型/白背景)**: パトレイバーと同じく、大見出しの直後・キャンバス上部付近から原画パネルがブリードする2カラム構成だった。→ `renderCurrentLayout`の再構成が1案件だけの偶然ではなく、GAAATの情報帯型バナーに共通する型だと確認できた(追加のコード変更なし、既存修正の裏付け)。
- **グレンダイザー(オンライン販売・スポットライト額装型)**: ④テンプレートと同じ構図(暗背景+額装原画+背後の色付きスポットライト)。ただし実物のスポットライトは輪郭のはっきりした濃い円で、こちらの実装はグラデーションが弱く輪郭がぼやけていた。→ `renderSpotlightFrameTemplate`の`spot`(radialGradient)の不透明度・半径を調整し、実物に近い「輪郭のある円」に強化。
- **頭文字D(情報帯型の別バリエーション)**: 色帯を使わず、公式ロゴ風タイポグラフィ+右下に日付/会場という別構成もあると判明。ツールは案件ごとの公式ロゴ画像は扱えないため、これは差分として記録のみ(コード変更はしていない)。

参照した実バナー画像は比較検証用としてスクラッチパッドに保存し、リポジトリには含めていない。

### 幅広い案件での追加検証(2026-08-31)

さらに、キャプテン翼・PEANUTS・PAC-MAN・銀魂・BLEACHの実バナーも見て回った(サイボーグ009はPSDのみでサイズ超過のため未確認)。

- **①キャラクター切り抜き型のタイトルに縁取り+ドロップシャドウを追加**: キャプテン翼(黒縁+グラデ塗り)・銀魂(白フチの太字)・BLEACH(黒ベタ+白フチ、原画に食い込む配置)の3例すべてで、大見出しに濃い縁取りかドロップシャドウが入っており、平坦なグラデーション塗りっぱなしの例は無かった。→ `renderCutoutTemplate`の見出しに`strokeText`(黒・半透明・太め)+ドロップシャドウを追加し、グラデーション塗りの上から重ねるよう変更。
- **PEANUTS**は原画をそのまま使わず、額装したアートを実際の部屋に飾った写真+明朝体主体の落ち着いたレイアウトで、このツールの前提(1枚のキャラクター原画から自動生成)とは根本的に異なる方向性だったため、コード変更はしていない(将来「上品/ライフスタイル系」のテンプレートを増やす際の参考として記録のみ)。
- **PAC-MAN**の該当ファイルは商品写真(額装アートを壁に掛けた単体カット)で、タイトルや会期情報を含むバナーではなかったため比較対象外。
- **銀魂**は大見出しがキャンバス最上部で原画に直接重なり、日付もキャンバス最下部で原画に重なる、非常に大胆な「全面ブリード+文字オーバーレイ」構成だった。BLEACHも同様にタイトルが原画の角に食い込む配置。①テンプレートは現状「原画エリアを避けて左上に文字を置く」安全側の構成のままにしており、原画への重なりを前提にした再設計はリスクが大きいため今回は見送った(将来的な発展候補として記録)。

### Meta広告配信を前提としたテキストサイズの引き上げ(2026-08-31)

Meta(Instagram/Facebook)のフィード広告として配信する前提で、実際のフィード上での表示サイズ(1080px原寸ではなく、スマホでは数百px幅まで縮小される)を考慮し、6テンプレート全ての「主見出し以外」の文字サイズを軒並み引き上げた。

- 副題・正式名称・会期/会場・コピーライトなど、これまで14〜24px程度だった補助的なテキストを軒並み4〜8px引き上げ(コピーライトは概ね14→18〜20px、会期/会場の情報行は16〜20→20〜28px、帯のメインコピー/サブコピーも32〜36→38〜46pxに拡大)。
- ③壁掛けフレーム型の見出しは他テンプレートと比べて明らかに小さかった(最大46px)ため、最大64pxまで引き上げ。
- サイズ変更に伴い、テキストブロックの縦方向の余白(帯の高さ`bandH`、行間`lineH`など)も比例して調整し、はみ出しがないことを6テンプレート全てで目視確認。
- **副産物のバグ修正**: サイズ引き上げの過程で、`drawInfoLine()`(会場名・会期・価格などを1行に結合して描画する共通関数)に幅の上限が一切なく、フォントを大きくしたら①テンプレートの会期情報が原画にはみ出す実際の不具合が発生した。`drawInfoLine()`に`maxWidth`引数を追加し、指定幅を超える場合は自動でフォントを縮小(最大70%まで)するようにして修正。他3テンプレートの同種の呼び出し箇所にも同じ上限を追加済み。

### ⑤サイバーUI型のタイトルを全面的に強化(2026-08-31)

ユーザーから「このテンプレも(展示会)タイトルが小さすぎる」と直接指摘があった。実際、⑤だけは展示会タイトルが18pxの等幅フォント1行タグ(`[ EXHIBITION ] タイトル`)のみで表現されており、フィード広告として縮小表示されるとほぼ判読不能だった(他5テンプレートは前段の対応で見出しをきちんと拡大済みだったが、⑤は「タイトル＝小さなHUDタグ」という意図的なデザインのままだったため取り残されていた)。

- `[ EXHIBITION ]` という小さなキッカーラベル(等幅フォント、HUD感を維持)はそのまま残しつつ、その下に他テンプレート同様のサイズ感(最大58px)で展示会タイトル本文を独立した見出しとして追加。
- **実装中に発見した実際の不具合**: タイトルを大きくしたところ、右上のCTAバッジ(価格・オンライン販売タグなど)とタイトルの1行目が重なって両方とも読めなくなった。原因はCTAバッジの幅計算をタイトル描画の後に行っていたため、タイトル側がバッジの存在を考慮せず全幅を使って折り返していたこと。バッジの寸法計算をタイトルより先に行うよう並べ替え、タイトルの折り返し幅をバッジの左端で頭打ちにするよう修正。あわせて、バッジ用に`ctx.font`を再設定する行が抜けていて(タイトル描画時に`ctx.font`が上書きされたまま)バッジのテキストが変な太さ/フォントで描かれる不具合も同時に修正。
- 中景の装飾テキスト「35.68N 139.76E」は新しいタイトルの2行目と位置が被るため削除(他のHUDフレーバーテキストは残置)。
- 長いタイトル(2行折り返し)・短いタイトル・CTAバッジの文字数違いなど複数パターンで実際にレンダリングして重なりがないことを確認。

### `fitFontSize`が「実際にはみ出さない」ことを保証していなかった問題(2026-08-31、複数箇所で実際に発生)

ユーザーが自動翻訳で実際にイタリア語のバナーを複数生成し、「全体的に修正が必要かも」とスクリーンショットを共有してくれたことで発覚。

**根本原因**: `fitFontSize()`はフォントサイズを`minSize`まで縮小して、そこで諦めて返すだけの実装で、縮小してもまだ収まらない場合にテキストが実際に指定幅を超えて描画されることを一切防いでいなかった。日本語の短いラベルでは問題にならなかったが、翻訳後の文字列(特にイタリア語などは日本語の2〜3倍の文字数になりがち)だと、最小サイズまで縮めても1行に収まらないケースが現実に発生する。

**実際に発生した不具合(3箇所)**:
1. ③壁掛けフレーム型: 下段の「作品名(左)＋CTAピル(右)」のうち、作品名が長いタイトルで右側のCTAピルに完全に重なり、両方とも判読不能なテキストの塊になった。
2. 情報帯型(現行レイアウト): 開催会場名が左カラムの幅を超え、右側の原画にはみ出して重なった。
3. ②縦書きタイトル型: 縦書きタイトル(横書きフォールバック)が想定より大きくレンダリングされ、下部のCTA帯を突き抜けてキャンバス下端まで到達した。この3つ目の原因は少し特殊で、`fitFontSize`自体は正しく直したはずが実際には直っておらず、原因は別にあった(次項参照)。

**対応**:
- `fitFontSizeTruncate()`という新しいラッパー関数を追加。`fitFontSize()`で縮小してもまだ指定幅を超える場合、末尾を「…」で省略して確実に幅内に収める。戻り値は`{size, text}`で、描画には必ず`.text`(切り詰め後の文字列)を使う。
- ①③④情報帯型・②縦書きタイトル型の、原画や別要素と衝突しうる全ての単一行タイトル/CTA/会場名の`fitFontSize`呼び出しを`fitFontSizeTruncate`に置き換え(合計8箇所)。
- ③壁掛けフレーム型の「作品名」ラベルは、そもそも上部の大見出しと完全に同じ内容を繰り返しているだけの冗長な要素だったため、パッチで延命せず削除。CTAピルが行全体の幅を使えるようにし、ピル自体も`fitFontSizeTruncate`で幅を保証。

**②のテンプレートだけ直らなかった理由(测定方法の不一致)**: `fitFontSizeTruncate`を適用したにもかかわらず②の横書きフォールバックだけ症状が再現し続けた。原因は、文字間隔(letter-spacing)付きで描画する`drawSpacedText()`が「1文字ずつ`measureText`して幅を積算する」方式なのに対し、`fitFontSize`/`fitFontSizeTruncate`は「文字列全体を1回`measureText`する」方式で幅を見積もっており、この2つの測定方法の結果が(特にOswaldのようなコンデンス書体で)大きく乖離していたこと。文字列全体としての測定は文字ごとの測定の合計よりかなり小さく出ることがあり、fitFontSize側は「収まっている」と判定したのに、実際に`drawSpacedText`で描画すると大幅にはみ出す、という食い違いが発生していた。`measureRunWidth()`という共通の幅測定関数を追加し、`drawSpacedText`と全く同じ「1文字ずつ積算」方式に統一。`fitFontSize`/`fitFontSizeTruncate`はこの関数経由でのみ幅を測るように変更し、以後は測定方法の不一致が起きないようにした。
- 3箇所とも、実際に長いイタリア語文字列(`state.currentLang`をその言語に正しく設定した状態)でレンダリングし直し、はみ出し・重なりが解消したことを画像で確認。

## 未翻訳フィールドの警告(2026-08-31 実装)

**報告された不具合**: 英語を含む複数言語で書き出したところ、展示会タイトル・メインコピーが日本語のまま出力された(会期情報の日付表記は正しく英語形式になっていた)。

**原因**: 展示会タイトル/メインコピー/サブコピーは「言語別」がデフォルトONの per-language フィールドで、新しい言語タブを初めて開いた瞬間に(`loadDraft()`内で)現在の言語のテキストがそのまま「訳す前の下書き」としてコピーされる仕様。ここで実際に訳文へ書き換えないまま一括生成やダウンロードを行うと、コピーされた原文がそのまま出力される — かつエラーも警告も一切出ない。ユーザーが英語タブを開いただけで「翻訳した」と思い込みやすい、静かな罠だった。

**対応(1回目)**: `findUntranslatedFields()`を追加し、設定済みの全言語を横断して、タイトル/メインコピー/サブコピー(コピーライトは意図的に除外— 複数言語で同一表記のままにしたいケースが多いため)のテキストが2言語以上で完全一致していないかチェックする。1件でも見つかれば、実際の書き出し操作(PNGダウンロード・一括生成・レイヤーZIP・PSD)の直前に`confirm()`で警告し、キャンセルすれば書き出しを中止できる。

**再発**: 上記の`confirm()`は書き出し直前の一度きり・ダイアログを閉じれば消える性質上、読まずに閉じられて素通りしてしまい、6言語すべて未翻訳のまま書き出される事象が実際に再発した。

**対応(2回目、恒久対策)**: 書き出し時の一度きりの警告に加えて、`scanTranslationGaps()`(旧`findUntranslatedFields`のコア判定を切り出したもの)を使い、「言語」欄の各言語チップに、まだ未翻訳の可能性がある言語には常時「！」バッジを表示するようにした(`renderLangBar()`を拡張)。バッジはタイトル/メインコピー/サブコピーの入力中もリアルタイムに更新される(該当3フィールドに`input`リスナーを追加し`renderLangBar()`を都度呼び出す)。あわせて言語欄の下に「未翻訳の可能性: 展示会タイトル（日本語・Englishが同じ内容）」のように具体的な項目名を列挙するヒント文も常時表示するようにし、編集中ずっと目に入る形にした。書き出し時の`confirm()`警告はそのまま残しているので、二重の安全網になっている。

`javascript_exec`で「言語を追加しただけで未編集→全チップに！バッジ+ヒント文が出る」「1言語ぶん翻訳→そのチップだけバッジが消える」の両方をライブ更新込みで確認済み。

## 要素の非表示・会期情報の編集・調整範囲の拡大・タイトル欄の見直し(2026-09-01 実装)

ドラッグ機能・Undo/Redoの実装直後、ユーザーから4件まとめて要望があった。①要素の削除（例: サブコピーを丸ごと消したい）、②会期情報も編集したい、③年（2026）を任意で消したい、④展示会タイトルの初期値がパトレイバー基準で特殊、⑤原画の移動範囲をもっと広げたい。着手前に、案件マスタ「案件一覧マスタ」スプレッドシート（`一覧`シート、企画タイトル列）を50件以上目視確認し、事実確認をしてから対応した。

### ①要素の非表示(削除)

**発覚した実際の不具合**: サブコピーを空欄にしても、③④②⑤（フレーム型/スポットライト型/縦書き型/サイバーUI型）では`saleCtaFallback()`等の代替文言（例:「ONLINE STORE」）が自動的に入ってしまい、実際には消せなかった（情報帯型・①切り抜き型は元々空欄で非表示になる作りだった）。テンプレートによって挙動がバラバラだったのを、明示的な「非表示」フラグで統一。

**対応**: `state.adjustments`の各カテゴリに`hidden`フラグを追加し、レイアウト微調整パネルに「非表示」列（チェックボックス）を追加。

- ロゴ以外（コピーライト・タイトル・メインコピー・サブコピー・会期情報・原画）は`LAYER_ORDER`ですでに1項目1レイヤーに分かれている(`compositeLayers()`が`LAYER_ORDER`を順にflattenしている)仕組みを利用し、「非表示」はそのレイヤーのcompositeだけをスキップする方式にした。描画自体（`recordBounds`含む）は普段通り実行されるので、(a) レイヤー分けZIP/PSD書き出しには影響しない（Photoshop側で普通に復活できる）、(b) 非表示中でもキャンバス上でドラッグして位置を仕込んでおける、という2つの副次的なメリットがある。
- ロゴだけは`decoration`レイヤーを常時表示の帯・スクリム・HUD枠と共有しているため、上記のレイヤーcompositeスキップ方式が使えず、5箇所の描画呼び出し自体を`if (!logoAdj.hidden) { ...drawImage... }`で個別に囲む方式にした。
- 「この案件の調整をリセット」は`hidden`も含めて全項目を初期値に戻す。Undo/Redoは既存の`state.adjustments`丸ごとスナップショット方式にそのまま乗るので追加対応不要だった。

**動作確認**: オンライン販売モードでメインコピー・サブコピーを両方空にした状態（＝フォールバック文言「ONLINE STORE」が出るはずの状態）で③のサブコピー枠を非表示にし、該当領域のピクセル合計値が非表示前後で変化することを確認（フォールバック文言が実際に消えていることの確認）。

### ②③会期情報の編集(自由入力)・年の表示切り替え

**対応**: 「会期情報」を7つ目の調整対象カテゴリとして追加（位置・サイズ・非表示すべて対応）。加えて、テキスト項目に以下2つを新設。

- 「会期情報の表示テキスト（任意・自由入力）」— 空欄なら従来通り開始日・終了日から自動生成、何か入力されていればその文字列がそのまま会期情報として使われる（通期開催や「随時」など、開始日・終了日のペアでは表現できないケースに対応）。自由入力時は、情報帯型の年ラベル・区切り線（開始日と終了日をつなぐ斜め線）は意味を失うため自動的に非表示になる。
- 「年（例: 2026）を表示する」チェックボックス — 情報帯型でのみ意味を持つ（他5テンプレートはもともと年を表示していない）。オフにすると年の行が消え、その分レイアウトが詰まる。

**位置・サイズ調整の実装範囲**: ②（縦書きタイトル型）と情報帯型は日付ブロックが単独で描画されているため、独立して位置・サイズを動かせる。③④①⑤（フレーム型/スポットライト型/切り抜き型/サイバーUI型）は`buildInfoLineParts()`+`drawInfoLine()`で会場名・会期・価格を1本の行にまとめて描画する設計のため、「会期情報」の位置調整はその行全体（会場名・価格も含む）を動かす形になる。会期だけを独立して動かすには行の分割描画が必要で、実装コストに対して効果が薄いと判断し、この4テンプレートでは「行全体が一緒に動く」仕様であることをパネルのヒント文で明記した。

**動作確認**: 自由入力テキストへの切り替えで日付ブロックの高さ・幅が想定通り変わること（情報帯型: 年表示ON時153.6px→年OFF時110.4px→自由入力時58pxの1行）、サイバーUI型で位置・サイズ調整（scale 150% / dx 40 / dy -20）が反映されること、非表示化で該当領域のピクセル合計が変化することを確認済み。

### ⑤原画（および全カテゴリ共通）の調整範囲の拡大

**実際の制約**: ドラッグ中の`mousemove`リスナーが`canvas`要素にしかバインドされておらず、カーソルがキャンバスの表示領域（1080pxのネイティブ座標に対し画面上は数百px程度に縮小表示）の外に出た瞬間、それ以上ドラッグが追従しなくなり「途中で動かなくなる」状態だった。数値入力欄には元々上限がなかったため、範囲が実質的に狭かったのはドラッグ操作だけだった。

**対応**: ドラッグ追従用の`mousemove`リスナーを`canvas`から`window`に変更（ホバー表示用の`mousemove`は従来通り`canvas`のまま分離）。あわせてホイールでのサイズ調整のクランプ範囲を10〜400%から5〜1000%に拡大。

**動作確認**: `mousedown`後にキャンバス表示領域の外（-2000px相当）まで`window`に対して`mousemove`をdispatchし、dx/dyがクランプされず正しくスケール換算された値まで追従することを確認。ホイール200回転相当のイベントでscaleが900%まで、さらに追加で1000%の上限にクランプされることを確認済み。

### ④展示会タイトルの初期値をパトレイバー基準から標準的な長さへ変更

**確認内容**: ユーザー指示に基づき、案件マスタ「案件一覧マスタ」（`https://docs.google.com/spreadsheets/d/1pjSfRYR17r1onw8RU3f-M1RsIDBpUUljqnV0zjgt-XU/`）の`一覧`シート、企画タイトル列を50件以上目視確認。実際の企画タイトルはほぼ全て1行の短い文言（例:「頭文字D ART EXHIBITION by GAAAT」「石ノ森章太郎『サイボーグ009』展」「キャプテン翼 ONE WORLD ONE TEAM ART EXHIBITION」）で、複数行にまたがる特殊な構成（大見出し／英字タグライン／正式名称の3段）を使うケースはパトレイバー展（デザイン参照元バナーの元ネタ）くらいしか見当たらなかった。

**対応**: タイトル欄の初期値を、3行構成のパトレイバー実例（"PATLABOR" / "THE MOBILE POLICE" / "Mobile Police Patlabor - Art of GEN【RELOAD】"）から、単一行の一般的な例（"GINTAMA ART EXHIBITION by GAAAT"）に差し替え。ラベルとヒント文も「通常は1行でOK。2行目・3行目は必要な案件だけの任意欄」という位置づけが伝わるように書き換えた（2行目＝英字タグライン／3行目以降＝正式名称という機能自体は残している。パトレイバーのような特殊案件では引き続き使える）。

### 上記の実装直後に発見・修正した不具合(2026-09-01、ユーザー指摘+全体レビュー)

ユーザーから「この巨大なスラッシュが気になる」との指摘を受け、それをきっかけに全テンプレート・新機能の組み合わせを一通り再検証し、他にも実際にレンダリングが壊れる不具合を複数発見・修正した。

1. **情報帯型の日付スラッシュ接続線が異常に大きい**: 開始日・終了日の2行の間を結ぶ斜め線が、2行分の高さ（約100px）をそのままカバーする形で描かれており、隣接する58pxの日付テキストに対して明らかに太すぎ・長すぎだった。2行の中間点を中心に、日付テキストのキャップハイトに近いサイズ（フォントサイズの0.84倍）へ縮小。
2. **原画を非表示にすると②⑤（縦書きタイトル型/サイバーUI型）で完全に透明な穴が開く**: この2テンプレートは全面原画のフルブリード構成で、原画の背後に単色の背景レイヤーが無かった（「原画未アップロード時」のプレースホルダー用の背景塗りが原画と同じ`artwork`レイヤーに同居していたため、非表示化するとその塗りごと消えていた）。`background`レイヤー（常に表示される）に単色背景を独立して敷くよう修正。
3. **会期情報の自由入力テキストに長さ制限が無く、情報帯型・②縦書きタイトル型でキャンバス外/原画上にはみ出す**: 実際に27〜30文字程度の自由入力（「2026年8月21日（金）〜9月30日（水）会期中無休・最終日は17時閉場」）を試したところ、情報帯型では原画の上に文字が乗り上げ、②では文字列がキャンバスの下端（1080px）を超えて画像の外まで続いていた。開始日・終了日から自動生成する日付表示は常に短い（「08.21 (Fri.)」程度）ため元々問題にならなかったが、自由入力はどんな長さもあり得るため、`fitFontSizeTruncate`で幅に収まらなければ縮小→省略（「…」）するガードを追加した。③④①⑤（`drawInfoLine`使用）は元々70%までの自動縮小を持っていたため、同じ文字列でも問題なかった（追加対応不要と確認）。
4. **②縦書きタイトル型のタイトルが長いと自力でキャンバス外に流れ出る**: 上記③のテスト中に偶然発覚。CJK（日本語/中国語）用の縦書きタイトルは文字ごとに積み上げる独自の縮小ロジックを持っていたが、フォントサイズの下限（32px）が32文字クラスの長いタイトル（今回のデフォルト値変更で採用した"GINTAMA ART EXHIBITION by GAAAT"が該当）に対して大きすぎ、下限まで縮小してもなお必要高さがキャンバス高さ(1080px)を超え、バナー下端からはみ出していた（実測: 985.8px分の高さ、開始位置102pxを足すと1087.8pxで1080pxを8px弱超過）。他の`fitFontSizeTruncate`利用箇所と同様、縮小だけでなく最終手段としての省略（「…」）を追加し、確実に収まるようにした。あわせて、縮小判定の計算式に実際の行送り係数(1.06倍)が抜けていた誤差も修正。

いずれも実際に該当条件でレンダリングし、要素の座標・高さがキャンバス範囲内に収まること、および画像として目視でも崩れていないことを確認済み。

## 一括生成前のサムネイル一覧プレビュー(2026-09-01 実装)

**背景**: 直前の全体レビューで、「特定の都市・言語の組み合わせだけ会期情報がはみ出す」といった、個別にテキストを見ているだけでは気づきにくい不具合が複数見つかった。一括生成は都市×言語の全パターンをまとめてPSD/ZIPにして納品まで進んでしまうため、こうした見落としがあると手戻りが大きい。そこでユーザーから「一括生成前のサムネイル一覧プレビュー」の実装依頼があった。

**対応**: 「案件マスタから貼り付け」フィールドセットに「プレビューを確認」ボタンを追加（既存の「一括生成してZIPダウンロード」ボタンの上）。押すと、選択中の都市×言語の全組み合わせを実際にレンダリングし、縮小サムネイルをモーダルのグリッドで一覧表示する。

- 一括生成の本処理(`runBatchGenerate`)は「セッション（都市）を切り替え→言語ごとにレンダリング→キャンバスを読み取る」というループを持っていたが、そのループ本体を`iterateBatchVariants(sessions, langs)`という非同期ジェネレータに切り出した。プレビューと本生成の両方がこの同じジェネレータを消費する（`for await`で1バリアントずつ受け取り、本生成は`canvas.toBlob()`でPNG化してZIPに、プレビューは240×240の縮小canvasに`drawImage`してサムネイル化）ため、「プレビューでは大丈夫に見えたのに実際の生成結果は違う」という食い違いが構造的に起こり得ない。
- サムネイルは実際のキャンバス内容をその場で縮小するだけなので、都市×言語が数十パターンあっても比較的高速に生成できる（PNGエンコードを伴う本生成より軽い）。
- 各サムネイルはクリック可能で、押すとその都市・言語の組み合わせに編集画面がその場で切り替わり（`applySession()`+`switchLang()`）、モーダルが閉じる。プレビューで見つけた問題をすぐさま本編集画面で直せるようにするため。
- 生成中に閉じる（✕ボタン／背景クリック／Escapeキー）と、以降のバリアントのレンダリングを打ち切る（見えないところで計算を続けても無駄なため）。閉じ方によって編集画面の状態を戻すかどうかが変わる: サムネイルをクリックして遷移した場合はその遷移を維持し、それ以外（✕・背景・Escape・自然完了）ではプレビュー開始前の会場名・会期・言語にきっちり戻す。
- 本生成側にある「未翻訳テキストへの警告」`confirmProceedWithUntranslated()`は、プレビュー側では意図的に呼んでいない。未翻訳のままのテキストがどう見えるかをまさに目で確認してもらうための機能なので、ここで警告ダイアログに遮られては本末転倒なため。

**動作確認**: 3都市×2言語=6パターンでプレビューを実行し、全6件のサムネイルが正しい会場名・日付・言語で生成されることを確認。サムネイルクリックでその都市・言語に画面が切り替わり編集フィールドが実際に書き換わることを確認。クリックせずに閉じた場合はプレビュー前の状態（会場名・開始日・言語）に正確に戻ることを確認。生成の途中（6件中2件目）で閉じるとそれ以降のレンダリングが打ち切られ、以後追加されないことを確認。リファクタ後の本生成（`runBatchGenerate`）自体も、ダウンロード処理をスタブして正常に完走しZIPが1回だけ生成されることを確認済み。

## 翻訳済みテキストの「原文変更後」検知(2026-09-01 実装)

**背景**: 既存の「未翻訳フィールドの警告」（`scanTranslationGaps()`）は、ある言語のテキストが他の言語と一字一句同じ（＝案件マスタからコピーされた下書きのまま、まだ訳文に書き換えられていない）場合だけを検知する仕組みだった。しかし、一度翻訳した後に日本語の原文だけを後から修正するケースは検知できなかった——翻訳済みの英語などは原文と一字一句異なる文章になっているため、「同じ内容かどうか」だけを見る既存のチェックでは引っかからない。ユーザーからこの「変更後」検知の実装依頼があった。

**対応**: `state.translationSnapshots`（`{ [言語]: { [フィールド名]: その訳文を書いた時点の原文テキスト } }`）を追加し、原文（日本語があれば日本語、無ければ最初に設定した言語＝`getSourceLang()`）の現在のテキストとスナップショットを比較する`scanStaleTranslations()`を新設した。

- スナップショットは2つのタイミングで記録される: ①自動翻訳（`autoTranslateUntranslated()`）が実際に翻訳した直後、②言語別フィールド（タイトル/メインコピー/サブコピー）を、原文の言語以外を表示中に手で編集した時（人力で訳文を書いた/直した、という確認の意味）。どちらの場合も「その時点の原文テキスト」をスナップショットとして保存する。
- `scanStaleTranslations()`は、スナップショットが記録されている言語・フィールドについてのみ、現在の原文テキストとスナップショットを比較する。スナップショットが無い（＝一度も翻訳・確認されたことが無い）場合は判定対象にしない——それは既存の`scanTranslationGaps()`（一字一句同じ＝未翻訳）が担当する領域であり、二重に警告を出さないようにするため。
- 検知結果は既存の「！」バッジ・翻訳警告ヒント文・書き出し前の確認ダイアログ（`confirmProceedWithUntranslated`）すべてに合流させた。「未翻訳」と「原文変更後」は文言を分けて表示する（例:「サブコピー（Englishが原文の変更前のまま）」）。
- 「未翻訳の項目を自動翻訳」ボタンは「未翻訳・要更新の項目を自動翻訳」に改称し、`scanTranslationGaps()`の未翻訳ジョブと`scanStaleTranslations()`の要更新ジョブを同じキューにまとめて処理するようにした。検知だけして直す手段が無いのでは片手落ちになるため、既存の自動翻訳の仕組みにそのまま乗せる形で「検知→ワンクリックで再翻訳」まで完結させた。

**動作確認**: 日本語→英語で自動翻訳（実際にMyMemory APIを呼び出し）→スナップショット記録を確認→日本語タイトルを編集→`scanStaleTranslations()`が英語タイトルを検知することを確認→再度自動翻訳して解消されることを確認。あわせて、自動翻訳を使わず英語欄を手で直接編集した場合もスナップショットが記録され、その後の日本語編集で正しく「要更新」判定されることを確認済み。書き出し前の確認ダイアログ用の`findUntranslatedFields()`にも反映されることを確認。

## 四隅ドラッグでのリサイズ(2026-09-01 実装)

**背景**: ドラッグでの移動・ホイールでのサイズ変更は実装済みだったが、ユーザーから「四隅をドラッグでサイズ変更できるようにしたい」との要望。デザインツール（Figma/PowerPoint等）でおなじみの、選択枠の角をつまんで拡大縮小する操作感が欲しいとのこと。

**対応**: ホバー中に表示される既存のハイライト枠（`#adjustHighlight`）に、四隅の小さいハンドル（`.resize-handle`、CSS絶対配置の12px四方）を追加。ハンドルをドラッグすると、そのハンドルが動いた分だけ`scale`（既存の数値入力・ホイールと同じプロパティ）を変化させる。

- リサイズの基準は「要素のバウンディングボックスの中心から、ドラッグ中の角までの距離」。ドラッグ開始時にその距離を記録しておき、ドラッグ中の距離が伸びれば拡大、縮めば縮小という比率で`scale`を決める（`newScale = startScale × (現在距離 / 開始距離)`）。dx/dyには触れず、あくまで数値入力・ホイールと同じ「スケールを変える」操作の別の入力手段という位置づけ——要素ごとに実際の描画アンカー（中央揃え/左揃えなど）が違うため、「つまんだ角の反対側を固定してリサイズ」という厳密な挙動をテンプレート横断で正しく再現するのは大掛かりになりすぎると判断し、既存のスケール概念に統一した。
- ハンドルは`pointer-events:auto`（親の`.adjust-highlight`自体は引き続き`pointer-events:none`）。ドラッグ判定は`state.elementBounds`頼みのヒットテストではなく、ハンドル自体が実DOM要素としてマウスイベントを受け取る素直な仕組み。
- 実装中に見つけた副作用: キャンバス上でホバー中の要素の枠が見えている状態からハンドルへマウスを動かすと、ハンドルは`canvas`要素の上に重なる兄弟要素のため、ブラウザは「`canvas`からマウスが離れた」と判定して`mouseleave`を発火する。これに反応してハイライトを隠す実装のままだと、ハンドルにマウスを乗せた瞬間に枠自体が消えてしまい掴めない、という不具合が起きるところだった。`mouseleave`の監視対象を`canvas`単体から親の`#canvasWrap`（キャンバスとハイライト枠の両方を含む）に変更し、キャンバス⇄ハンドル間の移動では発火しないようにして回避した。
- ドラッグ中の状態管理（`resizeState`）・Undo/Redoへの統合（`recordAdjustmentChange()`/`commitAdjustmentUndoGroup()`）・画面外までドラッグを追従させる`window`バインドのmousemoveは、既存の移動ドラッグ（`dragState`）とまったく同じパターンを踏襲。

**動作確認**: SE角（右下）を中心から遠ざける方向にドラッグしてscaleが100→243に増えることを確認。Undoで100に復元後、NW角（左上）を中心から遠ざける方向で186に増加、中心に近づける方向で118まで減少と、4隅とも方向が正しく効くことを確認。ドラッグ中に`canvas`の`mouseleave`だけを発火させてもハイライトが消えないこと（`#canvasWrap`を離れた場合のみ消えること）を確認。既存の移動ドラッグ・ホイールリサイズ・6テンプレート全ての通常レンダリングに影響が無いことも確認済み。

## タイトルフォントの選択(2026-09-01 実装)

**背景**: これまで展示会タイトル（および⑤サイバーUI型のキャッチコピー）のフォントは`TITLE_FONT_STACK`定数で固定（Oswald＋Noto Sans JP）だった。ユーザーから「フォントを変更したい場合はどうするか」という相談があり、案としてカスタムフォントファイルのアップロードより先に、あらかじめ用意した数種類から選ぶドロップダウン方式を提案・合意。作品の世界観に応じて数クリックで印象を変えられるようにした。

**対応**: `TITLE_FONT_PRESETS`（key→{label, stack}）を5種類定義し、「テキスト項目」フィールドセットに「タイトルフォント」ドロップダウンを追加。選択は`state.titleFont`（プロジェクト単位、テンプレート/配色と同様に全言語・一括生成で共通）に保存され、`render()`の先頭で`TITLE_FONT_STACK`（`const`→`let`に変更）をそのプリセットの値に差し替える。各テンプレート内の描画コードは元々ローカル変数`TITLE_FONT_STACK`を参照しているだけなので、32箇所以上ある呼び出し側は一切変更不要だった。

- プリセットは5種類: 太字ゴシック（デフォルト、Oswald＋Noto Sans JP）／明朝・上品（Playfair Display＋Shippori Mincho）／丸ゴシック・ポップ（Fredoka＋M PLUS Rounded 1c）／サイバー・近未来（Orbitron＋Noto Sans JP）／シンプル・ゴシック（Inter＋Noto Sans JP）。いずれも「デザイン性のあるLatin書体を先頭、CJKグリフをカバーする同系統の書体を2番目」という既存のOswald+Noto Sans JPと同じ並び順（ブラウザのフォント代替がグリフ単位で先頭から探すため、日本語部分だけ自動的に2番目のフォントで描画される）。
- Google Fontsは`<head>`の`<link>`タグ1本にfamilyパラメータを追加する形でまとめて読み込むようにした（プリコネクト設定は既存のものをそのまま利用）。
- **実装中に発見・修正した不具合**: `<link>`タグでフォントを読み込み可能にしただけでは、実際にそのフォントがブラウザにダウンロードされるとは限らない——`<canvas>`への`fillText()`は（実DOM上のテキストと違って）フォント読み込みを自発的にトリガーしない仕様のため、一度もDOM上で使われていないフォントは`ctx.font`に指定してもダウンロードされないまま代替フォントで静かに描画されてしまう。`document.fonts.check()`で確認したところ、既存のOswald/Noto Sans JP（読み込みから今まで実際に使われ続けていたので既にダウンロード済み）はtrueだったが、新規追加した6書体は全てfalseだった。対応として、ページ読み込み時に全プリセットの全書体に対して明示的に`document.fonts.load()`を発行し、完了ごとに`render()`し直すようにした。この過程で二段階の不具合があった: 最初のバージョンはプリセットの先頭フォント（Latin側）だけをロードしており、5プリセット中3つ（先頭以外の2番目＝CJK側が偶然すでに読み込み済みのNoto Sans JPと共通していたもの）はたまたま問題なく動いていたが、Shippori MinchoとM PLUS Rounded 1c（2番目のCJK側が他プリセットと共有されていない2つ）だけが未読み込みのまま残っていた。全8書体を直接列挙してロードする形に直して解消した。

**動作確認**: `document.fonts.check()`で8書体すべてが読み込み済み（true）になることを確認。実際に5プリセット全てで同じ英語タイトル文字列をレンダリングし、書体が明確に異なって描画されることを画像で確認（Oswaldの太い縦長ゴシック、Playfair Displayの優雅なセリフ、Fredokaの丸みを帯びたポップ体、Orbitronの近未来的な幾何学体、Interのシンプルなゴシック体）。非デフォルトのフォント選択状態で6テンプレート全てが正常にレンダリングされることも確認済み。

## 「年を表示する」チェックボックスの削除、および自由記入欄「追加テキスト」の追加(2026-09-01 実装)

**背景**: ユーザーから「年（例: 2026）を表示するチェックボックスは一度も使ったことがないので削除でいい」との指摘。加えて「このテキスト項目（タイトル/メインコピー/サブコピー/コピーライト/会期情報）以外にも入れたいものが将来出てくるかもしれないので、任意で追記できる項目があるといい」との要望があった。特定用途の小さなトグルを都度追加するより、汎用的な自由記入欄を先に用意しておく方が筋が良いという判断に合意し、実装した。

**対応①: 年表示チェックボックスの削除**: 情報帯型にしかなかった「年（例: 2026）を表示する」チェックボックスと、それに紐づく`yearLabel`の計算・描画コード（`renderCurrentLayout`内、日付ブロックの直上に小さく表示していた部分）を完全に削除した。年を表示したい場合は、既存の「会期情報の表示テキスト（自由入力）」に直接「2026.08.21 (金) - 08.24 (月)」のように年を含めて書けば表現できるため、機能の欠落にはならない。

**対応②: 「追加テキスト」欄の追加**: 展示会タイトル/メインコピー/サブコピー/コピーライトと同じ土台（`LANG_FIELD_GROUPS`・`state.langSpecific`・言語別トグル・下書き保存/復元）に「追加テキスト」を追加し、さらに「会期情報」等と同じレイアウト微調整の対象（`state.adjustments.extraText`: サイズ/位置/非表示、ドラッグ・四隅リサイズ対応）にもした。用途を限定しないテキスト欄で、空欄なら何も表示されない。

- 言語別切り替えに対応（ユーザーに確認済み: 訳文が必要な文言を入れるケースを想定）。一方で翻訳チェック（`scanTranslationGaps`/`scanStaleTranslations`/「！」バッジ）の対象からは意図的に外した——コピーライトと同じ理由で、ハッシュタグやURLなど「言語間で同じ表記のままにしたい」使い方が多いと想定されるため、意図的に同一表記でも警告を出さないようにしている。
- レイヤー分け出力にも対応するため`LAYER_ORDER`に`extraText`レイヤーを追加し、`LAYER_TO_ADJUSTMENT`にも登録——既存の「非表示チェックでレイヤーごとcompositeをスキップする」仕組みにそのまま乗るだけで済んだ。
- 6テンプレート全てに描画コードを追加。デフォルトの表示位置は「コピーライトのすぐ上（同じ角、1行分上）」に統一した——テンプレートごとに空いているスペースがまちまちで衝突しない位置を個別に判断するのは大掛かりになりすぎるため、既存の「小さい注記が集まる一等地」であるコピーライト付近に揃えることで、初期状態でもある程度自然に収まるようにした（気に入らなければドラッグ/四隅リサイズでどこへでも動かせる）。④のRTL（アラビア語）ミラーリングもコピーライトと同様に対応。

**動作確認**: 6テンプレート全てでエラーなく描画され、`state.elementBounds.extraText`が妥当な座標を持つことを確認。言語別切り替え（日本語で入力→英語タブに切り替えてシードされたテキストを別内容に編集→日本語タブに戻っても元の内容のまま）を確認。非表示チェックでピクセル差分が実際に変わることを確認。ドラッグで位置が正しく変わることを確認。全8カテゴリ（ロゴ/コピーライト/タイトル/メインコピー/サブコピー/会期情報/追加テキスト/原画）を同時に非表示にしても6テンプレート全てがクラッシュしないことを確認済み。

## タイトルの視認性向上(2026-09-01 実装)

**背景**: ユーザーから「タイトルをもっと目立つようにしたい。他のコピーと似たような感じで出すよりも」との指摘。実データで検証したところ原因が判明した——情報帯型（デフォルト）で実際によくある長さのタイトル（例:「GINTAMA ART EXHIBITION by GAAAT」32文字）は、既存の自動縮小ロジックにより約49pxまで縮んでいた一方、帯のメインコピーは42px（太さ800）。サイズ差がほぼ無く、装飾も無いプレーンな塗りのみだったため「他のコピーと似た感じ」に見えていた。他5テンプレートも程度差はあるが同様の傾向があり、①切り抜き型は32文字級のタイトルが省略記号で切られるケースすらあった。

**対応**: 「縮小し続ける」のではなく「最大2行（②縦書きタイトル型は2列）まで折り返して大きいサイズを保つ」方式に、6テンプレート全ての展示会タイトルを切り替えた。

- 新設の`fitFontSizeWrap(text, maxWidth, weight, family, startSize, minSize, spacing, maxLines, byChar)`が中核。既存の`fitFontSizeTruncate`と同じ「指定サイズ範囲で確実に収まることを保証する」設計を踏襲しつつ、1行に収まらなければ複数行へ折り返す点が異なる。行の詰め込みは`measureRunWidth`（文字間隔を考慮した実測）を使うため、`drawSpacedText`で実際に描画される幅とズレない。それでも`maxLines`行に収まらない場合は最終手段として省略記号で切る。
- プレーンな塗りだけだった情報帯型・③フレーム型には、うっすらとしたドロップシャドウを追加して視覚的な重みを足した（④スポットライト型・①切り抜き型・②縦書き型・⑤サイバーUI型は元々ストローク/グラデーション/シャドウ等の装飾があったため変更なし）。
- **②縦書きタイトル型は特殊対応が必要だった**: 縦書き（1文字ずつ縦に積む）は「1文字＝固定の行送り幅」で描画するため、`fitFontSizeWrap`の（実測幅ベースの）詰め込みロジックをそのまま「縦」に転用すると、幅の狭い欧文文字が行送り幅より大幅に多く1列に詰め込まれてしまい、実際に描画すると2倍近い高さにあふれる不具合が起きた（日本語モードのまま32文字の英語タイトルをテストして発覚。高さが828pxの想定に対し実際は1819px近くまであふれてキャンバス外に出ていた）。この不具合は、既存の「原文言語と現在選択中の言語タブが食い違うと縦書きの判定を誤る」という、このセッションで既に一度発見・修正した不具合と同根。縦書き用には文字数ベースで列を詰める専用ロジックに書き直し、2列目は1列目の左に配置（伝統的な縦書きの読み順と同じ）。欧文タイトルの回転済み1行表示も同様に2列（2本の平行な回転済みテキスト）に対応させた。
- ⑤サイバーUI型は元々2行折り返し止まりの独自実装だったが、3行目以降を省略記号無しで黙って消してしまう不具合があったため、他と同じ`fitFontSizeWrap`に統一して解消した。

**動作確認**: 実際の長いタイトル文字列（32文字クラス）で6テンプレート全てをレンダリングし、`state.elementBounds.title`がキャンバス範囲内（0〜1080）に収まることを確認。短いタイトル（5文字）では従来通り1行のまま無駄に折り返さないことを確認。②縦書きタイトル型は日本語（実際に長い日本語タイトル）・英語の両方の言語で2列表示が正しく機能し、高さがキャンバス内に収まることを確認。画像として目視でも、情報帯型で約49px→約95px相当まで文字が大きくなり、帯のメインコピーとの差が明確になったことを確認。ドラッグ・非表示トグル・6テンプレート全ての通常レンダリングへの影響が無いことも確認済み。

## ホイールでのリサイズ廃止・書き出し前プレビューの追加(2026-09-01 実装)

**背景**: ユーザーから2件の要望。①「スクロールでテキストや素材の大きさが変わるのは止めたい」——四隅ドラッグでのリサイズを実装済みのため、キャンバス上で要素にマウスが乗った状態でページをスクロールしようとすると意図せずリサイズされてしまう挙動が不要かつ邪魔になっていた。②「エクスポート前のプレビューは入れたいな」——PNG/レイヤー別ZIP/PSDのいずれのダウンロードボタンも、クリックすると（未翻訳・原画未アップロードの確認ダイアログを除けば）即座にファイルがダウンロードされる仕様で、最終チェックの機会が無かった。

**対応①**: `canvas`の`wheel`イベントリスナー（ホイールで`.scale`を増減する処理）を完全に削除。四隅ドラッグリサイズが既にあるため機能的な欠落は無い。副次的な効果として、`wheel`ハンドラが`evt.preventDefault()`していたことでキャンバス上でのページスクロールそのものも阻害されていた問題も同時に解消した（ハンドラが無くなったので通常のスクロール動作に干渉しない）。

**対応②**: PNGダウンロード・レイヤー別ZIPダウンロード・PSDダウンロードの3つの書き出しボタン全てに、実行前に確認モーダル「書き出し前プレビュー」を挟んだ。モーダルは現在のキャンバスをそのまま表示し、「キャンセル」で何もせず閉じる、「この内容で書き出す」で実際の書き出し処理（既存の未翻訳/原画未アップロード確認ダイアログを含む）を実行する。実装は`showExportPreview(actionFn)` / `closeExportPreview()`という汎用の2関数で、どの書き出しボタンからも同じモーダルを使い回している。閉じ方（✕ボタン・背景クリック・Escapeキー・キャンセルボタン）は全て同じ`closeExportPreview()`に集約。

**動作確認**: PNGダウンロードボタンを押すとモーダルが開き、実際のファイルダウンロードがまだ発生していないことを確認。キャンセルすると閉じてダウンロードが起きないこと、再度開いて「この内容で書き出す」を押すと（`window.confirm`をスタブして）実際にダウンロードが1回だけ発生し正しいファイル名になることを確認。レイヤー別ZIP・PSDボタンも同じモーダルが開くことを確認。背景クリック・Escapeキーでも閉じることを確認。ホイールイベントを要素上で発火させても`.scale`が変化せず、かつイベントが`preventDefault`されない（＝通常のスクロールを妨げない）ことを確認。四隅ドラッグでのリサイズは引き続き正常に動作することも確認済み。

## 書き出し前プレビューの全言語・全会期対応、Googleログインボタンの追加設置(2026-09-01 実装)

**背景**: ユーザーから2件の要望。①「プレビューは全言語、全会期分確認できるようにしたい」——直前に実装した「書き出し前プレビュー」は現在表示中の1件（1言語×1会期）しか見せておらず、既存の「一括生成プレビュー」（`showBatchPreview`）は複数言語をまとめて見られるが、案件マスタから会期情報を貼り付けていない（＝`parsedSessions`が空の）単一案件では、そのボタン自体が表示されず全言語プレビューの手段が無かった。②「Googleでログインボタンは案件マスタから貼り付けの方にもつけようかな」——スプレッドシート読み込み機能もGoogleログインが必要だが、ログインボタンは「キャラクター原画」欄にしか無く、案件マスタ欄から使い始めたユーザーはボタンを探して下にスクロールする必要があった。

**対応①**: 「書き出し前プレビュー」を単一画像表示から、「一括生成プレビュー」と同じサムネイルグリッド表示に作り替えた。中核は既存の`iterateBatchVariants()`ジェネレータの再利用——案件マスタから会期情報が貼り付けられていれば全会期×全言語、貼り付けられていなければ`getSelectedBatchSessions()`が空配列を返すので、代わりに空オブジェクト`{}`を1件だけの暗黙セッションとして渡す（`iterateBatchVariants`は`session.venue`等が無ければ何も上書きしないので、「今入力されている会場名・会期をそのまま使う」という意味になる）。これにより、案件マスタ貼り付けの有無に関わらず必ず全言語（＋全会期があれば全会期）のグリッドが出るようになった。

- 一括生成プレビューとの違い: あちらはサムネイルクリックで即座に切り替えて閉じる「ブラウズ用」。書き出し前プレビューは「これから書き出す1件を選ぶ」ための画面なので、サムネイルクリックでは切り替えつつモーダルは閉じず、クリックしたサムネイルに`selected`のハイライト枠を付けるようにした。ボタンを押した時点で表示されていた組み合わせ（言語・会場が一致するもの）がデフォルトで選択済みになっており、何もクリックしなければその場で「選択中の内容で書き出す」を押すだけで元々表示していた内容がそのまま書き出される。
- グリッド生成中にステートを都市×言語ぶん切り替えていくため、生成完了後は必ず元の状態（呼び出し時点の言語・会場・会期）に復元する——ただしサムネイルをクリックしていた場合はその選択が有効なままなので、復元後も見た目上の「選択済み」ハイライトと実際にエクスポートされる内容は一致する。
- `pendingExportAction`（実際の書き出し処理: PNG生成/ZIP生成/PSD生成のいずれか）は変更なし——常に「その時点のライブなキャンバス/state」に対して実行されるので、グリッドでどれを選んでいても正しく反映される。

**対応②**: 「案件マスタから貼り付け」フィールドセットの先頭に2つ目の「Googleでログイン」ボタンを追加。`initGoogleAuthUI()`を、単一のボタン/ステータス要素を直接触る実装から、`driveSignInBtn`/`sheetsSignInBtn`の配列を一括更新する実装に変更——ログイン成功時は両方のボタンが同時に「ログイン済み」表示になる。エラー・ポップアップブロック時のステータスメッセージは、実際にクリックされた方のボタン直下（`lastAuthStatusEl`で追跡）に表示するようにし、押していない方のボタンの場所に紛らわしいメッセージが出ないようにした。

**動作確認**: 単一言語・会期未貼り付け状態で書き出しボタンを押すと1件だけのグリッドが出ることを確認。3言語設定時に3件、2都市×2言語の会期貼り付け状態で4件、正しいラベル（会場名＋言語名）で生成されることを確認。サムネイルクリックで`state.currentLang`が切り替わり、選択ハイライトが移動し、モーダルは閉じたままであることを確認。クリックせずに「書き出す」を押すと元の状態でエクスポート処理が呼ばれること、クリックして切り替えた場合はその切り替え後の言語でエクスポート処理が呼ばれることを、`downloadPsd`をスタブして実引数レベルで確認。キャンセルでダウンロードが発生しないことも確認済み。Googleログインボタンは、新設したボタンをクリック→（OAuthコールバックを直接発火させて）ログイン成功をシミュレートすると両方のボタンが「ログイン済み」表示に切り替わることを確認。エラーレスポンス時は直近にクリックした方のボタンのステータス欄にのみエラーが表示されることも確認済み。

## タイトルの改行位置を手動指定できる「｜」記法(2026-09-01 実装)

**背景**: ユーザーから実際のスクリーンショット付きで指摘。「GAAAT銀魂藝術展」というタイトルが、直前に実装した自動2行折り返し（`fitFontSizeWrap`）により「GAAAT銀魂藝」「術展」と、「藝術展」という単語の途中で割れてしまっていた——文字数ベースの貪欲な折り返しは意味的な区切りを考慮しないため起こりうる問題。「GAAAT銀魂」「藝術展」で改行したい、という要望。

**対応**: 展示会タイトルの1行目に半角または全角のパイプ文字（`|` / `｜`。日本語IMEでは全角になりやすいため両方を受け付ける）を入れると、そこが強制的な改行位置として扱われるようにした。例:「GAAAT銀魂｜藝術展」→「GAAAT銀魂」「藝術展」の2行。

- 中核は`fitFontSizeWrap()`内で先頭に追加した分岐: テキストに`|`/`｜`が含まれていれば、貪欲な自動折り返しを一切行わず、パイプ区切りの各セグメントをそのまま各行として扱う。サイズは「全セグメントが指定幅に収まる最大サイズ」を`startSize`から`minSize`まで探索して決定し、それでも収まらないセグメントがあれば個別に省略記号で切る（自動折り返し版と同じ保証レベル）。
- ②縦書きタイトル型のCJK分岐（1文字ずつ縦に積む・列送り幅が固定という理由で`fitFontSizeWrap`を使わず専用の文字数ベースのロジックを持つ）には、同じ`|`/`｜`記法を個別に実装——`fitFontSizeWrap`本体の変更だけではここには波及しないため。
- 「テキスト項目」の展示会タイトル欄のヒント文に記法を追記。

**動作確認**: 「GAAAT銀魂｜藝術展」を6テンプレート全てでレンダリングし、意図通り「GAAAT銀魂」／「藝術展」で改行され、キャンバス範囲内に収まることを画像で確認（情報帯型・②縦書き型で目視確認）。全角「｜」・半角「|」・英語タイトルでの改行（「GINTAMA ART|EXHIBITION by GAAAT」）・パイプ無しの通常タイトルの計4パターン×6テンプレート＝24通りを`state.elementBounds.title`のオーバーフロー判定で確認し、全て正常。

## 調整プリセットの保存/読み込み（版元ごと）(2026-09-01 実装)

**背景**: 以前から提案していた機能。「レイアウト微調整」（ロゴ/コピーライト/タイトル/メインコピー/サブコピー/会期情報/追加テキスト/原画、計8カテゴリのサイズ・位置・非表示）は案件を閉じる（リロードする）と消える一時的な状態のまま——同じ版元から似た指摘が来るたびに、毎回イチから同じ調整をやり直す必要があった。

**対応**: `state.adjustments`丸ごとを名前付きでブラウザの`localStorage`に保存・復元できるようにした。「レイアウト微調整」フィールドセットの下部に「調整プリセット（版元ごとに保存・再利用）」セクションを追加。

- 保存: プリセット名を入力して「現在の調整を保存」。同名が既にあれば上書き確認（`confirm()`）。保存対象は`state.adjustments`のみ——テキスト内容・テンプレート・配色・原画などは含まない（版元の指摘は「レイアウトの直し」であって「文言の直し」ではないケースを想定した意図的なスコープ）。
- 読み込み: ドロップダウンから選んで「読み込む」。既存のUndo/Redoシステム（`recordAdjustmentChange()`/`commitAdjustmentUndoGroup()`）にそのまま乗せてあるので、読み込み自体もCtrl+Zで取り消せる。
- 削除: ドロップダウンから選んで「削除」（確認ダイアログあり）。
- 前方互換: 読み込み時は保存されたプリセットの中身をそのまま適用するのではなく、**現在の**`ADJUSTMENT_CATEGORIES`（8カテゴリ）を基準にマージする。今回のセッション中に「会期情報」「追加テキスト」カテゴリが後から追加された経緯があり、それより前に保存されたプリセット（あるいは将来カテゴリが増えた後の古いプリセット）を読み込んでも、無いカテゴリはニュートラルなデフォルト値で補われ、エラーにならず綻びなく適用される。
- `localStorage`保存のため、このブラウザ・このパソコンだけで有効（他の人とは共有されない、サーバー無しの設計を踏襲）。プライベートブラウジング等で保存に失敗した場合はエラーメッセージを表示する。

**動作確認**: 調整を変更→保存→リセット→読み込みで元の値に戻ることを確認。読み込み直後にCtrl+Z相当（`undoAdjustment()`）でリセット後の状態に戻ることを確認。`extraText`/`dates`を含まない旧形式のプリセット（手動で構築）を読み込んでも全8カテゴリが正しく埋まることを確認。同名保存時に確認ダイアログが出て、キャンセルすると上書きされず、承諾すると上書きされることを確認。削除でプリセットが消えることを確認。**ページを実際にリロードしてもプリセットが残っていること**（`localStorage`永続化の本来の目的）を確認済み。6テンプレート全ての通常レンダリングへの影響が無いことも確認済み。

---

## オンライン販売用バナーの情報を実制作物に合わせて刷新 (2026-09-01 実装)

**背景**: 「オンライン販売用」バナー用途は既に存在していたが、実際の見せ方は「会場名／会期情報」欄を「販売サイト名／販売期間」に読み替え、価格をそこにプレーンテキストで添えるだけの簡易的なものだった。GAAATの実制作物（Google Driveの見本フォルダ2件、R256「攻殻機動隊」・R220「焦茶」）を確認したところ、実際のECバナーは共通して次のパターンを持っていた:
- 「[IP名] アート作品 オンライン販売中」に相当する、目立つ色付きバッジ
- 「Small Edition｜¥44,000（税込）」のような価格・エディション情報を、白背景の目立つバッジとして表示
- 会場名・会期情報は一切使われない

ユーザーからのフィードバックを踏まえ、レイアウト構造（6テンプレートそれぞれの絵作り）自体は変更せず、この2つの「バッジ」表現を各テンプレートに追加する形で実装した。

**実装**:
- `saleTagField`（セールタグバッジ）という新しい自由入力・言語別テキスト項目を追加。「オンライン販売用」の時だけ表示される。空欄でも`saleTagFallback()`が「アート作品　オンライン販売中」を自動的に補う（IPアイコンタグ的に常に表示させたいという要望のため、ユーザーが明示的に自由入力を選択）。`subCopy`を使い回さない専用フィールドにしたことで、`subCopy`は集客用・販売用どちらでも「サブコピー」という一つの役割のままになる。
- 価格（`priceField`、既存）は`venue`テキストへのプレーンテキスト添付をやめ、`drawPill()`による白背景の丸バッジとして独立表示するように変更。
- 共通ヘルパー`drawPill(x, y, text, opts)`（角丸バッジ1個を描画）と`drawSaleBadges(x, anchorY, align, accentHex, fgHex, baseSize, anchor)`（セールタグ＋価格の2バッジをまとめてスタック描画、`anchor:'bottom'`/`'top'`両対応）を新設。
- 表示位置・サイズ調整は既存の`調整範囲`パネルに乗せる形にした: セールタグは既存の「会期情報」カテゴリ（`dates`）を流用し、価格バッジは新設の「価格バッジ（オンライン販売用）」カテゴリ（`venue`）を使う。どちらも「オンライン販売用」の時にしか描画されないので、集客用の「会期情報」欄・「会場名」欄の挙動には影響しない。`venue`は新しい調整カテゴリのため`ADJUSTMENT_CATEGORIES`・`state.adjustments`・`ADJUSTMENT_LABELS`・調整グリッドのHTML行を追加している。
- テンプレートごとに元々あったCTA表現（③フレーム型・④スポットライト型・②縦書きタイトル型の帯テキスト、⑤サイバーUI型の常設バッジ）は、オンライン販売モードの時だけテキストの出どころを`saleTagText()`に差し替える形で流用し、①切り抜き型・情報帯型（デフォルト）には新規にバッジ表示を追加した。

**不具合修正（実装中に発見）**: 価格バッジに「非表示」チェックを入れても実際には消えなかった（`compositeLayers()`のレイヤー単位の非表示判定が`LAYER_TO_ADJUSTMENT`に`venue`エントリを持たず、素通りしていたため）。`venue`を`LAYER_TO_ADJUSTMENT`に追加する案は、集客モードの会場名テキストまで巻き添えで非表示になってしまうため採用せず、`drawPill()`自体に`hidden`オプションを持たせ（`logo`の非表示処理と同じ、個別描画箇所での判定パターン）、価格バッジの描画呼び出し全箇所（4テンプレート×各1箇所）で`hidden: venueAdj.hidden`を渡すよう修正した。

**動作確認**: 6テンプレート全てで「オンライン販売用」に切り替え、タイトル・メインコピー・価格を入力してレンダリングし、セールタグバッジ（デフォルト文言／カスタム文言）と価格バッジが正しく表示されることをスクリーンショットで確認。キャンバス上の実際のmousedown/mousemove/mouseupイベントで価格バッジをドラッグしてもエラーが出ず位置が動くことを確認。「非表示」チェックのON/OFFで対応するレイヤーのピクセルが実際に消える/戻ることを確認。会場名テキストを入力してもバッジ行と視覚的に衝突しないことを確認。「集客用（来場促進）」モードに戻した際、日付・会場名・サブコピーが従来通り表示されバッジ関連の要素が一切描画されないこと（回帰確認）を6テンプレート全てで確認。調整プリセットの保存/読み込みが新しい`venue`カテゴリを含めて正しく往復することを確認。

---

### 上記の修正: 価格の自動生成禁止・販売終了日のみ・メインコピー/サブコピーの復活 (2026-09-01 追加修正)

ユーザーからのフィードバックで2つの問題が判明した:

1. **価格は絶対に自動生成しないこと**（実売価格と食い違うと誤解を招くため）。元々`priceField`に自動補完・デフォルト値は無かったが、誤解のリスクを明示するため`priceFieldLabel`を「価格・オファー文言（任意）」に、専用のヒント文（「空欄なら価格は一切表示されません。…金額は自動生成・自動補完せず…」）を追加して、UI上でも保証を明文化した。
2. **販売期間は「終了日のみ」・任意にしたい**（開始日は不要）。`dateStart`の入力欄自体を「オンライン販売用」の時は非表示にし（`dateStartWrap`）、`buildInfoLineParts()`・vertical2の縦書き日付ロジックの両方で、sale purposeの時は`dateOverride || dateEnd`のみを使うよう変更（開始日は一切読まない）。
3. **メインコピー・サブコピーが集客用と同様に入力できること**。実装当初、③フレーム型・④スポットライト型・⑤サイバーUI型は「CTAピル」1箇所をsubCopy/mainCopyと共用しており、そこをセールタグバッジ用に差し替えてしまったため、この3テンプレートでは販売用モードにするとsubCopy（またはmainCopy）が画面のどこにも表示されなくなっていた。CTAピルの中身は購入用途に関係なく常にsubCopy/mainCopyに戻し、セールタグ＋価格バッジは全テンプレートで**別の新しい表示枠**として追加する形に直した。

この3番目の修正に伴い、セールタグバッジ用に新しい調整カテゴリ`saleTag`（レイヤーも新設）を追加した。当初はセールタグを「会期情報」カテゴリ（`dates`）に間借りさせていたが、上記の日付表示の見直しにより「オンライン販売用でも販売終了日を表示したい」ケースが復活し、`dates`枠と`saleTag`枠が同時に使われる場面が生まれたため、同じレイヤー・調整キーを共有すると非表示チェック/ドラッグ操作が競合してしまう。そのため`saleTag`を独立したレイヤー・調整カテゴリとして切り出した（`LAYER_ORDER`・`LAYER_LABELS`・`LAYER_TO_ADJUSTMENT`・`ADJUSTMENT_CATEGORIES`・`ADJUSTMENT_LABELS`・調整グリッドのHTML行を追加）。

**動作確認**: 6テンプレート全てで、mainCopy・subCopy両方に別々のテキストを入力した状態でレンダリングし、両方とも実際にキャンバス上（レイヤーのピクセル判定）に描画されていることを確認。特に③フレーム型は、集客モードとオンライン販売モードで「mainCopy非表示・subCopy表示」という挙動が完全に一致すること（=販売モードによる新たな欠落が無いこと）を確認。「オンライン販売用」に切り替えると開始日入力欄が隠れ、ラベルが「販売終了日（任意）」に変わること、「来場促進（集客用）」に戻すと元通りになることを確認。新設した`saleTag`カテゴリも非表示チェック・実際のドラッグ操作（mousedown/mousemove/mouseup）が正しく機能することを確認。集客モードでの回帰（6テンプレート×レンダリングエラー無し、コンソールエラー無し）を再確認。

---

### さらなる追加修正: 折り返し禁止トグル・セールタグの任意化・バッジの右下配置 (2026-09-01 追加修正2)

ユーザーが実際に長めのタイトル（「焦茶アート作品」）を試したところ、「焦茶アート作\n品」のように意図しない位置で自動折り返しされるスクリーンショットが送られてきた。加えて3点の追加要望があった。

1. **「1行に収める」トグル**: タイトル・メインコピーそれぞれに「1行に収める（自動改行しない・収まらない分は自動縮小）」チェックボックスを追加（`state.titleNoWrap`/`state.mainCopyNoWrap`、プロジェクト単位の設定で`state.drafts`には含めない＝タイトルフォント選択と同じ扱い）。オンにすると、タイトルは全テンプレート共通で`fitFontSizeWrap(...)`の`maxLines`を2→1に強制。メインコピーは4テンプレート（情報帯型・④スポットライト型・①切り抜き型・⑤サイバーUI型）でそれぞれの複数行ラップ処理を`fitFontSizeTruncate(...)`（縮小→最終手段で末尾省略）に差し替え。③フレーム型・②縦書きタイトル型のメインコピーはもともと1行表示（CTAピル／`fitFontSizeTruncate`）だったため変更不要。
2. **セールタグバッジも価格と同様に完全に任意化**: `saleTagFallback()`（空欄時に「アート作品　オンライン販売中」を自動で補う仕組み）を削除。`saleTagText()`は`els.saleTag.value.trim()`をそのまま返すだけになり、空欄なら本当に何も表示されない（priceTagTextと同じ挙動に統一）。プレースホルダー・ヒント文言もそれに合わせて更新。
3. **セールタグ＋価格バッジを右下（CTAボタン風）に再配置**: 「セールタグバッジはCTAボタンチックだから右下らへんがいい」というフィードバックを受け、6テンプレート全てでバッジの配置を変更した。
   - 情報帯型（デフォルト）・②縦書きタイトル型: 帯（カラーバンド）の右下、コピーライトの上。帯の背景色そのものがaccentHexのため、タグピルは白背景＋accent色文字（`drawSaleBadges`の`accentHex`/`fgHex`引数を入れ替えて再利用）にして帯に埋没しないようにした。
   - ③フレーム型: 既存のsubCopy/mainCopy CTAピル（右下・右寄せ）のすぐ上にスタック。
   - ④スポットライト型: 右下（Arabic等RTL言語では左下にミラー、`isRtl`分岐を流用）、コピーライトの上。
   - ①切り抜き型: ロゴ＋コピーライトのクラスター（右下、原画の上にスクリム込みで乗る）のさらに上。バッジはピル自体が単色背景を持つため、スクリムが無くても視認性は保たれる。
   - ⑤サイバーUI型: コピーライトの上、HUD風フレームの右下角。

②縦書きタイトル型は、バッジが帯の右側に来たことで、中央寄せだったメインコピー／サブコピーの帯テキストが長い場合にバッジと重なる問題が新たに見つかった（実機で確認）。オンライン販売モードでバッジが表示される時だけ、帯テキストの最大幅を260px狭め、中心位置を左に130pxずらして回避。加えてバッジ自体もこのテンプレートの帯（高さ130px、他テンプレートの帯より低い）に収まるよう、2つのピルのフォントサイズを22→15に縮小した。

「ECは案件マスタが無い」というコメントもユーザーから受け取った——案件マスタ（会場・会期のスプレッドシート取込）は集客用データが前提の仕組みで、オンライン販売用バナーのテキストは元々全て手入力である、という前提の共有として記録に留め、UI変更は行っていない。

**動作確認**: 「焦茶アート作品」タイトル＋長めのメインコピーで「1行に収める」をONにし、6テンプレート全てでタイトル・メインコピーが1行に収まること（縮小・最終手段で省略はあり得るが折り返しはしない）をスクリーンショットで確認。セールタグを空欄にした状態でレンダリングし、`saleTag`レイヤーにピクセルが一切乗らない（=何も描画されない）ことを確認、価格側は独立して引き続き表示されることも確認。逆に価格だけ空欄・セールタグだけ入力したケースも確認。6テンプレート全てでバッジが右下（またはテンプレート内の空いているコーナー）に収まり、他の要素（コピーライト・CTAピル・メインコピー帯テキスト）と視覚的に衝突しないことをスクリーンショットで確認、②縦書きタイトル型で発見した重なりはその場で修正して再確認。バッジの非表示チェック・実ドラッグ操作（mousedown/mousemove/mouseup）が新しい配置でも問題なく動作することを確認。集客モードへの回帰が無いことも6テンプレート全てで再確認。

---

### さらなる追加修正3: 省略記号の廃止・販売終了日の「まで」表記・販売サイト名と案件マスタ貼付の削除 (2026-09-01 追加修正3)

上記の「1行に収める」を実際に長いタイトルで試したところ、`fitFontSizeWrap`/`fitFontSizeTruncate`が最小フォントサイズ（例: タイトルは通常時の40〜64px）まで縮小してもなお収まらない場合に「…」で末尾を省略する仕様のままだったため、「GINTAMA ART EXHIBITION by GAAAT」のような長い英字タイトルで実際に省略記号が出た。ユーザーから「バナー上でテキスト省略はわけわからん」というフィードバックを受け、方針を変更した。

1. **省略記号を実質廃止**: 「1行に収める」がONの時だけ、最小フォントサイズを大幅に下げた（タイトル: 40〜64px→14px、メインコピー: 16〜24px→10px）。UIのリスト等と違いバナーは静止画で省略された文字を確認する手段が無いため、見た目が多少小さくなっても全文表示を優先する。
2. **販売終了日に「まで」を付与**: 単体の日付「08.24 (月)」だけでは何の日付か分からないという指摘を受け、`formatSaleDeadline(dateLabel, lang)`を新設。日本語は末尾に「まで」、英語は先頭に「Until 」、他8言語もそれぞれの言い回しを前置（例: 中国語「至」、フランス語「Jusqu'au 」）。自由入力の会期テキスト（`dateOverride`）はそのまま（ユーザーが書いた文言を尊重）で、自動生成された終了日だけに適用。
3. **販売サイト名を廃止**: GAAATの実制作物でも使われておらず、「いらないかな」という判断を受けて「オンライン販売用」の時は開催会場名欄自体を非表示にし（`applyBannerPurposeUI`）、6テンプレート全ての描画コードでも`state.bannerPurpose==='sale'`の時は会場名テキストを一切描画しないよう修正（`buildInfoLineParts`・情報帯型・②縦書きタイトル型のそれぞれの会場名描画箇所）。
4. **案件マスタから貼り付けフィールドセットを非表示化**: 「オンライン販売用バナーは会期情報ごとに作成しないので案件マスタは不要」という指摘を受け、`案件マスタから貼り付け`セクション（Googleログイン・スプレッドシート取込・一括生成を含む）をオンライン販売用の時は丸ごと非表示にした（`sessionMasterFieldset`、コード自体は削除せず、集客用では引き続き利用可能）。

**動作確認**: 「GINTAMA ART EXHIBITION by GAAAT」＋長めの日本語メインコピーで「1行に収める」をONにし、`fitFontSizeWrap`/`fitFontSizeTruncate`の返り値に「…」が含まれないこと（プログラム的に検証）とスクリーンショットの両方で確認。販売終了日が「08.24 (月)まで」と表示されることを確認。オンライン販売用に切り替えると開催会場名欄・案件マスタセクションが非表示になり（`getComputedStyle().display`で確認）、来場促進（集客用）に戻すと両方とも元通り表示されることを確認。販売サイト名に値が入っていても（切り替え前に入力していた場合）、オンライン販売用ではバナー上に一切描画されないことを確認。6テンプレート全ての通常レンダリングへの回帰が無いこと、コンソールエラーが出ないことを確認。

---

### さらなる追加修正4: メインコピーの拡大ドラッグが効かない不具合・日付の浮き見え (2026-09-01 追加修正4)

ユーザーが「1行に収める」でメインコピーを大きくしようとキャンバス上で四隅ドラッグを試したところ、サイズが変わらなかった。原因は、`fitFontSizeTruncate(mainCopy, maxWidth, weight, family, 42 * mainCopyAdj.scale / 100, 10, 0)`のように、`state.adjustments.mainCopy.scale`（ドラッグで変化する値）を**縮小開始サイズ**としてそのまま関数に渡していたこと。この関数は指定した最大幅に収まるまでサイズを縮める仕組みなので、スケールを上げても「収まる最大サイズ」まで即座に縮められてしまい、見た目上は何も変わらなかった（テキストが既に横幅いっぱいまで自動フィットしているため）。

タイトル側（`fitFontSizeWrap`）は元々このバグが無かった——`scale`をフィット関数の引数には渡さず、フィット結果のサイズに対して`size = titleFit.size * titleAdj.scale / 100`と**後から掛け算**していたため、スケールを上げると素直にキャンバスからはみ出す方向に大きくなる（他の調整可能要素と同じ挙動）。メインコピーの「1行に収める」実装（4テンプレート×2箇所ずつ）を同じパターンに統一——**スケール100%固定でフィットさせたサイズ**を先に求め、それに対して`.scale`を後掛けするよう修正した。これによりドラッグで意図通り拡大縮小できるようになる（大きくしすぎると原画やキャンバス端にはみ出す点は他の要素と同様、許容されたトレードオフ）。

もう1点、情報帯型（デフォルト）で「日付が上に浮いてる」という指摘があった。原因は、販売終了日の表示位置が`venueBaseline`（廃止した会場名テキストの本来の位置）から一律40px上、という計算のままだったため、実際には描画されない会場名テキストの分の余白がそのまま宙に浮いた隙間として残っていたこと。会場名は「追加修正3」で完全に廃止済みなので、日付をロゴのすぐ上（会場名テキストが元々あった位置そのもの、`logoBottomY - 22`）に直接アンカーし直し、不要な余白を解消した。

**動作確認**: メインコピーのスケールを100%→200%に変えるシミュレーション（実際の四隅リサイズ操作と同じ計算式で再現）を行い、`state.elementBounds.mainCopy`の描画幅が正確に2倍になることを確認（修正前は変化しなかった）。スクリーンショットでも、スケール130%にした際にメインコピーが実際に大きく表示される（原画エリアにはみ出すのも含めて意図通り）ことを確認。情報帯型で日付がロゴの真上に詰まって表示され、不自然な余白が無いことをスクリーンショットで確認。6テンプレート全て（オンライン販売用・集客用の両方）でレンダリングエラー・コンソールエラーが無いことを再確認。

---

## キャンバス上のドラッグに中央スナップガイドを追加 (2026-09-01 実装)

**背景**: 「テキストをドラッグでバナーの中央に持ってきた時に中央線が出るようにしたい、Canvaみたいに編集のしやすさを上げるために」という要望。既存の四隅ドラッグ・移動ドラッグの仕組みに、位置決めの精度と手応えを上げるためのスナップガイドを追加した。

**実装**:
- `index.html`に`#centerGuideV`（縦線）・`#centerGuideH`（横線）を`#canvasWrap`内に追加。CSSで`left:50%`/`top:50%`固定なので、キャンバスの表示サイズが変わってもJS側でピクセル計算し直す必要がない。
- `applyCenterSnapAndGuides(key)`（新規関数）: 要素の現在のバウンディングボックス中心が、キャンバス中心（540, 540）から`CENTER_SNAP_PX`（6px、キャンバス座標系）以内に入ったら、その軸の`dx`/`dy`を中心にぴったり合う値まで自動調整（スナップ）し、再描画。スナップ後の実際の位置を基準にガイド線の表示/非表示を決める。
- 既存の移動ドラッグの`window`の`mousemove`ハンドラ内、`render()`の直後・`syncAdjustmentInputs()`の前に`applyCenterSnapAndGuides(dragState.key)`を呼ぶだけで組み込み。`mouseup`時に`hideCenterGuides()`でガイドを消す。
- リサイズ（四隅ドラッグ）には適用していない——要望が「ドラッグで中央に持ってきた時」という移動操作について限定的だったため、スコープを絞った。
- 判定はキャンバス全体（1080×1080）の中心のみが対象（要素同士の位置合わせではない）。GAAATの各テンプレートは要素配置がデザインされたレイアウトであり、Canvaのような自由配置キャンバスではないため、「他の要素との整列」までは実装せず、中央合わせという最も需要が高いユースケースに絞った。

**動作確認**: 実際のmousedown/mousemove/mouseupイベントで、コピーライト要素を横方向で中央付近までドラッグし、ガイド線（縦）が表示され、要素の中心x座標が正確に540になる（スナップが効く）ことを確認。同様に縦方向のスナップ・ガイド線（横）表示も確認。中央から明確に離れた位置へのドラッグではガイド線が一切表示されず、スナップも効かないことを確認（誤爆防止）。スクリーンショットでピンクの縦ガイド線が実際にキャンバス上に表示されることを視覚確認。ドラッグ終了（mouseup）でガイド線が消えることを確認。6テンプレート全ての通常レンダリングへの回帰・コンソールエラーが無いことを確認。

---

### 中央スナップガイドを他要素との整列にも拡張 (2026-09-01 追加実装)

**背景**: 「④スポットライト型」でサブコピー「全国5都市開催」とオンライン販売用の日付「08.24 (月)まで」を横に並べようとした際のスクリーンショットが送られてきて、「こんな感じでテキストを並列する際はドラッグしてる側を中央に配置できるようにしてほしい」「高さもぴったりになったらスナップするようにしてほしい」という要望を受けた。上記のキャンバス中央スナップを、**他の要素の中心との整列**にも一般化した。

**実装**:
- CSS: `.center-guide`のガイド線を`left:50%`/`top:50%`固定から、JS側で`style.left`/`style.top`を都度計算する方式に変更（キャンバス中央以外の位置にもガイド線を出せるようにするため）。
- `collectSnapTargets(excludeKey, axis)`（新規）: 指定軸（x/y）について、キャンバス中央（540）に加えて、**現在非表示になっていない他の全要素の中心座標**を候補として集める（ドラッグ中の要素自身と、非表示（`adj(key).hidden`）の要素は除外）。
- `nearestSnapTarget(current, targets)`（新規）: 候補の中から`SNAP_PX`（6px）以内で最も近いものを1つ選ぶ。
- `applyCenterSnapAndGuides(key)`を上記2関数を使う形に書き換え。ヒットした場合はdx/dyをその分だけ補正して吸着させ、`updateGuideLines()`でガイド線をヒットした座標（キャンバス中央でも他要素の中心でも）に表示する。
- これにより、「サブコピーの縦位置に日付を合わせたい」といった、要素同士を横並びの1行として揃えたいケースで、ドラッグするだけで自動的にぴったり合うようになる（縦・横どちらの軸にも対応、それぞれ独立して判定）。

**動作確認**: ④スポットライト型で、サブコピー「全国5都市開催」の隣に日付「08.24 (月)まで」をドラッグで持っていくシミュレーション（実際のmousedown/mousemove/mouseupイベント）を行い、日付要素の縦方向中心がサブコピーの縦方向中心とほぼ完全に一致する（誤差1px未満）ことを確認。その際に横ガイド線が正しい高さ（サブコピーの中心のY座標）に表示されることも確認。従来のキャンバス中央スナップ（縦・横とも）も、リファクタ後の共通コードパスで引き続き正常に動作することを再確認。スクリーンショットで、日付がサブコピーと同じ行に整列して表示されることを視覚確認。6テンプレート全ての通常レンダリングへの回帰・コンソールエラーが無いことを確認。

---

### スナップ判定を上端・下端にも拡張（中心だけでなく「揃った」「重なった」瞬間も対応） (2026-09-01 追加実装2)

**背景**: 情報帯型でロゴと日付を並べたスクリーンショットを受け、「高さを揃えた時や上と下のテキストが重なった瞬間などもスナップしてほしい」という追加要望。中心（center）同士の一致だけでは、「2つの要素の上端を揃える」「上下に積んだ要素の隙間をゼロにする（くっつける）」といった、実際のバナー編集でよくある整列操作をカバーできていなかった。

**実装**: `applyCenterSnapAndGuides`が使うスナップ候補を、要素ごとに中心1点のみから**上端・中心・下端（縦方向）／左端・中心・右端（横方向）の3点**に拡張。ドラッグ中の要素側も同じく3点の候補を持ち、`bestSnap(candidates, targets)`（新規）が全組み合わせ（3候補×対象ごとの3点）の中から`SNAP_PX`以内で最も近い1件を選ぶ。これにより、Figma/Canva等の「スマートガイド」と同様、以下がすべて同じ仕組みで実現される:
- 中心 対 中心（既存・維持）
- 上端 対 上端、下端 対 下端（「高さを揃えた」＝2つの要素の見た目の高さレベルを一致）
- 自分の上端 対 相手の下端、自分の下端 対 相手の上端（「上と下のテキストが重なった瞬間」＝隙間ゼロで積み重ねる）

ガイド線はスナップした対象の実座標に表示されるので、中心合わせでも端合わせでも見た目上の挙動は変わらない（線の位置がその都度変わるだけ）。

**動作確認**: 情報帯型で、日付要素をロゴ要素に向かって下方向にドラッグし、（1）日付の下端がロゴの上端にちょうど触れる位置でスナップすること（誤差1px未満）、（2）日付の上端がロゴの上端に一致する位置でスナップすること、の両方を実際のドラッグイベントで確認。どちらのケースでも横ガイド線が正しい座標に表示されることを確認。既存のキャンバス中央スナップ・6テンプレートの通常レンダリングに回帰が無いことを再確認。

---

### 四隅リサイズにもスナップを追加、その過程で発覚した「拡大ドラッグが効かない」根本原因を横展開で修正 (2026-09-01 追加実装3)

**背景**: 「テキスト同士の上辺を揃えて高さを揃えようと四角（＝四隅のリサイズハンドル）をドラッグしてもうまくできない」という報告。移動ドラッグには前項までで整列スナップを実装済みだったが、**四隅リサイズ（拡大縮小）にはスナップが一切無かった**ことに加えて、実装・検証の過程で「販売終了日の表示がある程度以上大きくならない」という別の不具合が見つかった。

**不具合の根本原因**: 情報帯型のオンライン販売用日付・自由入力の会期テキスト・②縦書きタイトル型の日付が、いずれも`fitFontSizeTruncate(text, maxWidth, weight, family, 32 * datesAdj.scale / 100, ...)`のように、**`.scale`を乗算した値をそのまま「縮小開始サイズ」として渡していた**。この関数は指定した最大幅に収まるまで自動的にサイズを縮める仕組みなので、一定以上`scale`を上げても「その文字列が指定幅に収まる自然な最大サイズ」まですぐに縮められてしまい、それより先はスケールを上げても見た目が一切変わらないという、以前メインコピーの「1行に収める」で見つけた不具合と全く同じパターンだった（該当箇所: 情報帯型のオンライン販売用日付、情報帯型の会期情報自由入力テキスト、②縦書きタイトル型の日付）。3箇所とも、メインコピーの時と同じ修正方針——**スケール100%固定でまず自然にフィットさせ、そのフィット結果に対して`.scale`を後から掛け算する**——に統一した。

**四隅リサイズのスナップ実装**: 移動ドラッグ用に作った`bestSnap`・`collectSnapTargets`をリサイズにも流用しつつ、`snapScaleToEdgeTarget(key, getEdge, target)`という新関数を追加。ドラッグしているのが北側（上）の角なら上端、南側（下）なら下端、東西でも同様に、対象の1辺だけをスナップ判定にかける（四隅リサイズは中心が固定されたまま一辺だけが動く操作のため）。閉じた数式で「目標の辺に到達するスケール」を逆算するのではなく、**実際に2回レンダリングして辺の位置を測定し、その傾き（スケール1%あたり何px動くか）から必要なスケールを逆算、最後にもう一度測定して補正する**という経験的な方式にした——これは、テンプレートによってテキストの位置決め方式が違う（中心を軸に対称に伸びる箱もあれば、日付のようにベースラインを軸に非対称に伸びるものもある）ため、単一の数式では通用しないと判明したため（実際、最初は中心対称を仮定した数式で実装したが、日付のベースライン方式の要素では全く機能しなかった）。横軸（左右端）の整列は、縦軸（上下端）が近くで見つからなかった時だけ試す（四隅リサイズはスケール1つで縦横が連動するため、両方を同時に満たせるとは限らない）。

**動作確認**: まず不具合の再現——`state.adjustments.dates.scale`を100→300に変えても情報帯型のオンライン販売用日付の描画サイズがほとんど変わらないこと（`fitSize`が一定値に張り付く）を確認してから修正、修正後はスケール300%で高さが正確に3倍（linearに比例）になることを確認。四隅リサイズのスナップは、実際のDOM（`.handle-se`要素への本物のmousedown/mousemove/mouseupイベント）を使い、日付要素を右下方向にリサイズしてロゴの上端に近づけ、下端がロゴ上端とほぼ完全に一致する（誤差0.02px未満）ことを確認。スクリーンショットでも、日付が大きく拡大されてロゴにぴったり隙間なく接する様子を視覚確認。ターゲットが無い（届く範囲に何も無い）場合はスケールが変化せず安全に何もしないことも確認。6テンプレート全て（オンライン販売用・集客用）でレンダリングエラー・コンソールエラーが無いことを確認。

---

### 「四角（リサイズハンドル）をドラッグすると他の要素まで動く」不具合を修正 (2026-09-01 追加実装4)

**背景**: 「テキストの四角をドラッグすると選択してない周りのテキストの場所が変わる」という報告。原因は情報帯型（デフォルト）の実装で、ロゴのすぐ上に会場名・日付を配置するための`logoBottomY`という変数が、**ロゴの実際の描画高さ（`tinted.h` = `40 * logoAdj.scale / 100`）から逆算されていた**こと。つまりロゴを四隅ドラッグで拡大・縮小すると`logoBottomY`が動き、そこを基準にしている会場名・日付の位置も一緒にズレてしまっていた。他の調整項目（サイズ・位置・非表示）は「この要素だけの上書き」という前提でずっと作ってきたが、ロゴだけこの前提が崩れていた。

**実装**: `logoBottomY`の算出を、ロゴの実際のスケール後の高さではなく、**固定の基準値（`logoBaseH = 40`、スケール100%相当）**から求めるように変更。ロゴ自体の描画・当たり判定（`recordBounds('logo', ...)`）は引き続き実際のスケール後サイズを使うので、ロゴ自身を拡大縮小する操作は今まで通り機能する。違いは、その拡大縮小が**もう他の要素の位置に影響しない**という点——ロゴを大きくしすぎて他の要素と重なった場合は、他の要素側の位置修正はユーザーの手動調整に委ねられる（他の要素をスケールで大きくする時と同じ「はみ出しても自己責任の上書き」という既存の方針に合わせた）。

念のため他の5テンプレートのロゴ描画箇所も全て確認したが、ロゴの高さ・幅を他要素の位置計算に使っているのは情報帯型だけで、他5テンプレートは元々ロゴが固定座標（左上・右上・右下など）に配置されており、この種の連鎖は発生していなかった。

**動作確認**: オンライン販売用・情報帯型で、ロゴのスケールを100%→250%（大きく）、100%→40%（小さく）と変えても、日付要素の`state.elementBounds.dates`（座標・サイズ）が一切変化しない（オブジェクトとして完全一致）ことをプログラム的に確認。スクリーンショットでも、ロゴを250%まで拡大すると見た目上ロゴが日付と重なる（＝ロゴ自体は正しく大きくなっている）ものの、日付テキストの位置・大きさ自体は元のまま動いていないことを視覚確認。6テンプレート全て（オンライン販売用・集客用）でレンダリングエラー・コンソールエラーが無いことを確認。

---

### ③④テンプレートの額装（フレームのモックアップ表示）を廃止 (2026-09-01 追加実装5)

**背景**: 「額は勝手につけないでほしい。実際に販売する額と違うと誤解を招くから。これは集客の方も同じね」というフィードバックを、当初「価格」の話だと誤解して対応していた。実際は「額装」（＝③壁掛けフレーム型・④スポットライト額装型で、原画をフレーム・マット・ガラス風の額に入れて壁掛け風に見せていた演出）のことだった。実際の展示・販売の額装（フレームの色・マットの有無・ガラスの有無など）と絵作り上のモックアップが食い違うと、閲覧者に誤解を与えかねない、という指摘。集客用・オンライン販売用のどちらにも当てはまるため、両方で対応。

**対応方針の選択**: 額装表示を「任意（チェックボックスでON/OFF）」にする案と、「廃止して原画をそのまま見せる」案を提示し、後者を選択。

**実装**: `drawPictureFrame(outer, accentHex)`（ドロップシャドウ・フレーム・アクセント縁取り・マット・ガラス反射・ベベルを描画し、内側の原画用矩形を返す関数）を`drawArtworkPlain(outer)`に置き換え。新関数は`outer`矩形に原画をそのままカバー表示するだけ（フレーム・マット・ガラス演出は一切無し）。③フレーム型・④スポットライト型の呼び出し箇所を両方とも新関数に差し替え。テンプレートのレイアウト自体（原画エリアの位置・サイズ、その他のテキスト配置）は変更していない——額装の演出だけを取り除いた。

あわせて、レイアウト選択のプルダウンのラベルも実態に合わせて更新: 「③壁掛けフレーム型(汎用・ギャラリー風)」→「③ギャラリー型(汎用・原画をそのまま表示)」、「④スポットライト額装型(ヒーロー・特撮・重厚系)」→「④スポットライト型(ヒーロー・特撮・重厚系)」（「額装」の文言を削除）。自動レイアウト選択ロジック内のコメントも整合性のため更新。

**動作確認**: ③④テンプレートを、原画未アップロード状態（プレースホルダーの点線表示）と、テスト用画像をアップロードした状態の両方でレンダリングし、スクリーンショットでフレーム・マット・ガラス反射が一切表示されず、原画（またはプレースホルダー）がそのまま矩形エリアいっぱいに表示されることを確認。オンライン販売用・集客用の両方で確認。6テンプレート全てでレンダリングエラー・コンソールエラーが無いことを確認。

---

### 「キャラクター原画」欄を「素材選定」に名称変更 (2026-09-01 追加実装6)

**背景**: 「キャラクター原画っていう項目は「素材選定」という項目に変更」という要望。ツールが集客用（展示会）だけでなくオンライン販売用（EC）にも対応するようになった経緯もあり、「キャラクター原画」というアニメ・キャラクターIP寄りの名称より、より汎用的な「素材」という呼び方に統一する。

**実装**: UI上でユーザーに見える箇所・書き出しファイルに含まれる箇所を中心に、「キャラクター原画」「原画」の表記を全て「素材」（セクション見出しは「素材選定」）に統一した。
- `index.html`: セクション見出し（`<legend>キャラクター原画</legend>` → `素材選定`）、ヘッダー下の説明文、レイアウト自動選択のラベル・ヒント文、配色抽出まわりのラベル・ボタン、コピーライトのヒント、調整グリッドの「原画（トリミング）」ラベル、レイヤー分け書き出しのヒント文、③テンプレートのプルダウン表記、案件マスタ欄でのGoogleログイン案内文中の言及。
- `app.js`: キャンバス上のアップロード誘導テキスト（プレースホルダー、5箇所）、素材未アップロード時のconfirm/alert/ステータス文言、`ADJUSTMENT_LABELS.art`（調整パネルでの表示名）、`LAYER_LABELS.artwork`（レイヤー分けZIP/PSD書き出し時のレイヤー名）、関連コメント。
- テンプレート名（「①キャラクター切り抜き型」など、原画のスタイル・世界観を指す分類名）は「素材」の呼び方とは別物のため変更していない。

**動作確認**: `grep`で`原画`の残存が無いことを確認。実ブラウザで6テンプレート全て（オンライン販売用・集客用）をレンダリングしエラー無し・コンソールエラー無しを確認。スクリーンショットで、左パネルの見出しが「素材選定」に、キャンバス上のプレースホルダーが「素材をアップロード」になっていることを視覚確認。

---

### 書き出し前プレビューに大きいプレビュー表示を追加 (2026-09-01 追加実装7)

**背景**: 「プレビューで選択したら大きく表示するようにしてほしい」という要望。既存の「書き出し前プレビュー」は都市×言語の全組み合わせを小さいサムネイルのグリッドで並べるだけで、文字校正するには小さすぎた。

**実装**: グリッドの上に、選択中の1件だけを原寸に近いサイズ（最大420px角）で表示する「大きいプレビュー」エリアを追加。モーダルを開いた時点のデフォルト選択（元々表示していた組み合わせ）でも、サムネイルをクリックして切り替えた時でも、常にその時点で`canvas`（実際の1080×1080描画結果）から直接`toDataURL()`で取得して表示するようにした——都市×言語ぶん（最大20件程度）の原寸画像を毎回全部メモリに持たせるのは無駄が大きいため、選択中の1件分だけをその場で取得する設計。

なお、「プレビューで確認して誤りがあった場合、修正したかったらどう対応すればいいんだっけ？」という質問への回答も兼ねて動作を確認・整理した: サムネイルをクリックすると、その都市×言語の内容が実際に本体側の入力欄（会場名・会期・言語タブなど）に反映される。その状態でプレビューを閉じる（✕・キャンセル・Esc）と、閉じた後もその内容のまま本体側に残る（自動では元の内容に戻らない）ので、そのまま本体側でテキストを直接修正すればよい——という一連の流れを確認できた。

**動作確認**: 実ブラウザで書き出し前プレビューを開き、デフォルトで大きいプレビューに選択中の内容（原寸に近い画像・都市名・言語ラベル）が表示されることを確認。別の言語のサムネイルをクリックすると、大きいプレビューの画像・ラベルが即座に切り替わり、そのサムネイルに`selected`クラスが付くことを確認。「キャンセル」で閉じた後も`state.currentLang`が最後にクリックした言語のままになっている（＝本体側の編集画面に反映されたまま）ことを確認——上記の「誤り修正フロー」が実際に機能することの裏付け。6テンプレート全てで通常レンダリングへの回帰・コンソールエラーが無いことを確認。

---

### デフォルト値の見直し: タイトル・メインコピーを1行表示に、初期言語を日本語のみに (2026-09-01 追加実装8)

**背景**: 「展示会タイトルやメインコピーはデフォルト1行表示にしよか」「英語がデフォルトで入ってるけど日本語だけにしてほしい」という2つの要望。どちらも初期状態（新しい案件を開いた直後）の挙動を、より実運用に近い形に変更するもの。

**実装**:
- `state.titleNoWrap`/`state.mainCopyNoWrap`の初期値を`false`→`true`に変更し、対応するチェックボックス（`titleNoWrapToggle`/`mainCopyNoWrapToggle`）にも`checked`属性を追加。ページを開いた直後から、タイトル・メインコピーは自動改行せず1行に収める（収まらない場合は自動縮小）動作になる。
- `state.languages`の初期値を`['ja', 'en']`→`['ja']`に変更。ページを開いた直後は日本語タブのみで、英語などが必要な案件では「言語」欄から手動で追加する運用になる。

**動作確認**: ページを新規に開いた直後の状態（`state.titleNoWrap`/`state.mainCopyNoWrap`/`els.titleNoWrapToggle.checked`/`els.mainCopyNoWrapToggle.checked`が全て`true`、`state.languages`が`['ja']`のみ）をプログラム的に確認。言語バーのDOM（`#langBar`）が実際に「日本語」タブ1つだけをレンダリングしていることを確認。6テンプレート全て（オンライン販売用・集客用）でレンダリングエラー・コンソールエラーが無いことを確認。

---

### 開催会場名・会期を独立してドラッグ/非表示できるように (2026-09-02 追加実装9)

**背景**: 「会場と会期情報切り離せるといいなと思いました！」という要望。③④①⑤の4テンプレート（`renderFrameTemplate`/`renderSpotlightFrameTemplate`/`renderCutoutTemplate`/`renderCyberUiTemplate`）は、開催会場名（またはオンライン販売用の価格バッジ）と会期を`buildInfoLineParts()`で1本のテキスト行として組み立て、共通の`drawInfoLine()`で1つの調整カテゴリ（`dates`）として描画していたため、ドラッグ・非表示の操作が両方に同時にかかってしまっていた。

**実装**: `drawInfoLine()`と同じ「1行として共有の折り返し・整列を計算する」レイアウトはそのままに、行を構成する各パーツ（venue/dates）がそれぞれ自分自身の調整カテゴリ（`useLayer`/`adj`/`recordBounds`/`hidden`）を持てる`drawVenueDateLine(parts, ...)`を新設し、上記4テンプレートの`drawInfoLine`呼び出しを置き換えた。行全体の基準位置は各パーツの「未調整時の幅」から計算するため、何もドラッグしていないデフォルト状態の見た目は変更前と完全に一致する——明示的にドラッグ/非表示にしたパーツだけがその基準位置からずれる。区切り記号（｜／など）は常に未調整位置の隙間に描画する。`ADJUSTMENT_LABELS.venue`を「価格バッジ（オンライン販売用）」から「開催会場名／価格バッジ」に変更し、調整パネルのラベルとヒント文でこの役割の二重化（集客モードでは会場名テキスト、オンライン販売モードでは価格バッジ）を説明。

**動作確認**: 実ブラウザで`frame3`テンプレートを対象に、`state.adjustments.venue.dx/dy`を動かしても`state.elementBounds.dates`がバイト単位で変化しないこと（逆も同様）、`venue.hidden`/`dates.hidden`をそれぞれ切り替えると対応するレイヤー（`layers['venue']`/`layers['dates']`）のアルファチャンネルだけが消えることをプログラム的に確認。全調整をリセットしたデフォルト状態のスクリーンショットで、変更前と同じ「会場名／会期」の1行表示になっていることを視覚確認。6テンプレート×2モードのレンダリングエラー・コンソールエラー無しを確認。

---

### タイトルの「英字タグライン・正式名称」専用欄を廃止し、Enterでの複数行入力に統一 (2026-09-02 追加実装10)

**背景**: 「企画タイトルはほとんどが1行の短い文言が多い」「2行目・3行目は英字タグラインや正式名称を添えたい特殊な案件だけで使う任意欄なので、この機能は削除で。その変更に伴ってEnterで改行できるようにしてほしい」という要望。従来は展示会タイトル欄で2行目・3行目に入力すると、`renderCurrentLayout`（情報帯型）と`renderCutoutTemplate`（①）の2テンプレートに限り、1行目とは別の小さいフォント・別レイヤー（タグライン／正式名称）として描画される特殊仕様になっていた。

**実装**: 上記2テンプレートから、2行目をタグラインとして描画する`if (lines[1])`ブロックと、3行目以降を正式名称として折り返し描画する`if (lines[2])`/`formalName`ブロックを削除。全6テンプレートで、タイトル欄のテキストは`els.title.value.trim()`をまるごと1つの文字列として扱い、テキストエリア内の改行（Enter）を、タイトルの折り返し計算関数`fitFontSizeWrap()`が既に持っていた半角「｜」と同じ「手動改行位置」として扱うように拡張（`fitFontSizeWrap`内の判定・分割の正規表現に`\n`を追加）。`renderVerticalTitleTemplate`（②）内の、CJKタイトルを文字数ベースで縦書きの列に振り分ける専用ロジックにも同様に`\n`を追加。結果として、Enterで改行した各行はすべてタイトルと同じ太字・同じ枠内で描画される（＝独立したタグライン欄ではなく、タイトル自体を複数行にする機能になった）。`index.html`のラベル・ヒント文からタグライン／正式名称欄の説明を削除し、「改行したい位置でEnterを押すか、半角の｜を入れてください」という案内に統一。

**動作確認**: `fitFontSizeWrap('LINE ONE\nLINE TWO', ...)`が`fitFontSizeWrap('LINE ONE|LINE TWO', ...)`と同じ`{size, lines}`を返すこと、3行以上を渡すと`maxLines`で正しく切り詰められることをプログラム的に確認。`renderVerticalTitleTemplate`でも`\n`区切りと`｜`区切りが同一の`elementBounds.title`を返すことを確認。6テンプレート×2モード×（1行タイトル／Enter区切り2行タイトル）の全組み合わせでレンダリングエラー無しを確認。`renderCurrentLayout`でEnter区切りの2行タイトルをスクリーンショットし、2行とも同じ大きな見出しスタイルで描画されている（旧タグライン仕様のような別スタイルの小さい2行目になっていない）ことを視覚確認。

---

### 情報帯型（デフォルト）・②縦型テンプレートでも開催会場名を独立して調整可能に (2026-09-02 追加実装11)

**背景**: 「開催会場名も会期情報やコピー同様に動かれたりサイズ変更できるようにしてほしい」という要望。追加実装9で③④①⑤テンプレートの会場名／会期を独立ドラッグ対応させたが、`renderCurrentLayout`（情報帯型・デフォルト）と`renderVerticalTitleTemplate`（②縦型）の2テンプレートは、そもそも会場名の描画に`adj('venue')`も`recordBounds('venue', ...)`も呼んでおらず、固定位置・固定サイズで一切ドラッグ・拡大縮小・非表示ができない状態だった（同じ関数内の会期・コピーライト・追加テキストなどは元々対応済み）。

**実装**: 両テンプレートの会場名描画ブロックに、他の要素と同じ`scale`（フィット後に掛け算する方式）/`dx`/`dy`/`hidden`対応を追加。実装中、`renderCurrentLayout`側で会場名の`dy`をドラッグすると、直下に描画される会期（`dates`）のベースライン計算がその会場名の`dy`込みの座標（`venueBaseline`）を直接参照していたため、会場名を動かすと会期まで一緒にずれてしまう連鎖バグを検出——ロゴの実スケールに会期側の位置計算が引っ張られていた過去の不具合（追加実装内の`logoBottomY`修正）と同じパターン。会期側の基準点を、会場名の`dy`を含まない固定値`venueBaselineBase`に分離することで解消（会場名自体の描画位置は従来通り`venueBaseline = venueBaselineBase + venueAdj.dy`のまま）。

**動作確認**: 実ブラウザで`default`/`vertical2`の両テンプレートについて、`state.adjustments.venue.dx/dy`を変更しても`state.elementBounds.dates`がバイト単位で変化しないこと、`venue.scale`を200にすると会場名の高さがちょうど2倍になること、`venue.hidden`にすると`venue`レイヤーのアルファチャンネルが全て0になることをプログラム的に確認。6テンプレート×2モードのレンダリングエラー無しを確認。

---

### タイトルフォントを5種類→10種類に拡充し、「おすすめ」「よく使う」を上位表示 (2026-09-02 追加実装12)

**背景**: 「選べるフォントをもっと増やしたいのと、よく使うフォントとおすすめフォントを上位に表示するようにしたい」という要望。

**実装**:
- `TITLE_FONT_PRESETS`に5種類追加（既存5種と合わせて計10種）: `impact`（極太インパクト・ポスター風／Anton+Noto Sans JP）、`comic`（コミック・レトロポップ／Bangers+Reggae One）、`brush`（筆文字・和風／Cardo+Yuji Syuku）、`pixel`（ドット・レトロゲーム風／Press Start 2P+DotGothic16）、`elegant`（モダン・エレガント／Poppins+Zen Kaku Gothic New）。各プリセットは既存と同じ「英字の個性的な見出し書体＋同じ雰囲気の和文書体」のペア構成。`index.html`のGoogle Fonts `<link>`にこれらのfamilyを追加。
- 各プリセットに任意の`recommended: true`フラグを追加できるようにし、`gothic`（デフォルト）・`impact`・`brush`の3つに設定（GAAATのアニメ・マンガ展示会バナーで汎用性が高いと判断したもの）。
- 「よく使う」はサーバー無し・このブラウザのみの集計: `titleFontSelect`で選択するたびに`localStorage`（キー`gaaat-banner-tool:title-font-usage`）へ選択回数を加算する`bumpTitleFontUsage()`を追加（既存の`gaaat-banner-tool:adjustment-presets`と同じ設計方針）。
- `#titleFontSelect`は静的な`<option>`列挙をやめ、`renderTitleFontOptions()`が起動時・選択変更のたびに`<optgroup>`3つ（「おすすめ」→「よく使うフォント」（選択回数の多い順、おすすめと重複除外）→「その他」）で組み立て直す方式に変更。選択中の値は再構築後も保持する。
- Canvasのフォント強制ロード処理（`document.fonts.load`を全プリセット分呼ぶ箇所）を、ハードコードされた配列から`TITLE_FONT_PRESETS`の各`stack`先頭2書体を自動抽出する方式に変更——今後プリセットを追加する際にこの配列を手動更新し忘れる心配がなくなる。

**動作確認**: 実ブラウザで10プリセット全てを`state.titleFont`に設定して`render()`しエラー無しを確認。`localStorage`をクリアした状態で`#titleFontSelect`のDOM構造が「おすすめ」（gothic/impact/brush）→「その他」（残り7件、宣言順）の2グループになることを確認。`bumpTitleFontUsage('pixel')`を3回・`bumpTitleFontUsage('clean')`を1回呼んでから`renderTitleFontOptions()`すると、「よく使うフォント」グループが`[pixel, clean]`（使用回数降順）で「おすすめ」と「その他」の間に挿入されることを確認。

---

### フォントを10種類→25種類に拡充し、Googleスライド風のプレビュー付きピッカーに変更 (2026-09-02 追加実装13)

**背景**: 「選べるフォントをもっと増やしたいのと、よく使うフォントとおすすめフォントを上位に表示するようにしたい」に続けて、「フォントのイメージはGoogleスライドのこんな感じでフォント名とどんなフォントがわかる表記にしたい」「Googleスライドくらもっと対応フォントを増やしてほしい」という要望。ネイティブ`<select>`は各`<option>`をブラウザごとに異なる書体で描画でき（Chromeは対応、Safari/Firefoxは非対応）、Googleスライドのような「一覧の各行がその書体そのもので描画される」プレビューを安定して出せないため、カスタムのドロップダウンUIに置き換えた。

**実装**:
- `TITLE_FONT_PRESETS`に15種類追加（既存10種と合わせて計25種）: `bebas`（コンデンス・スタイリッシュ）、`script`（手書き風スクリプト）、`classic`（クラシック・格調高い）、`blackGrotesk`（極太グロテスク）、`friendlyRound`（フレンドリー・丸ゴシック）、`markerPop`（マーカー手書き・ポップ）、`urbanBold`（アーバン・重厚感）、`slabHeavy`（極太スラブセリフ）、`condensedImpact`（コンデンス・見出し向け）、`engraved`（彫刻風・重厚）、`handwriting`（ナチュラル手書き）、`terminal`（レトロ端末・ドット）、`elegantThin`（上品・細字エレガント）、`typewriter`（タイプライター・ヴィンテージ）、`warmSerif`（温かみのあるセリフ）。既存分と同じ「英字の個性的な見出し書体＋同じ雰囲気の和文書体」ペア構成。`index.html`のGoogle Fonts `<link>`に対応familyを追加。
- `index.html`の`#titleFontSelect`（`<select>`）を廃止し、`#titleFontPickerBtn`（現在の選択をそのフォントで描画するトリガーボタン）＋`#titleFontPickerPanel`（`position:fixed`のドロップダウンパネル）の自作コンポーネントに置き換え。パネルの各行は本物のDOM要素（`<div class="font-picker-item">`）で、大きい行に「見出しラベル（例:「筆文字・和風」）をそのプリセットの`font-family`で描画」、小さい行に「実際の英字書体名／和文書体名」（例:「Cardo / Yuji Syuku」）を表示——Googleスライドの「フォント名をそのフォントで描画する」表記に、ペア構成の性質上「名前」を「ムード名＋実書体名」の2段に分けて対応させた形。選択中の項目には✓、グループは従来通り「おすすめ」→「よく使うフォント」→「その他」。
- DOM側のプレビューは通常の`<link>`スタイルシート経由でブラウザが自動でフォントを取得・差し替えるため、JS側での`document.fonts.load()`は不要（Canvasの場合とは異なり、実DOMテキストはブラウザが勝手に面倒を見てくれる）。一方Canvas側（バナー本体）は従来通り明示的な読み込みが必要なため、`loadCanvasTitleFont(key)`を新設し、選択された1プリセット分だけを読み込むように変更——25種類に増えたことで、旧実装のまま「起動時に全プリセット分を強制読み込み」を続けると誰も使わないフォントも含め最大50ファイルを毎回フェッチすることになるため、選択時オンデマンド読み込みに変更した（パネルを開くだけでは何もフェッチしない設計）。
- パネルの開閉: ボタンクリックでトグル、パネル外クリック・Escapeキー・ウィンドウリサイズ・サイドパネルのスクロールで閉じる。位置は`position:fixed`＋ボタンの`getBoundingClientRect()`から都度計算するため、`#panel`（サイドバー）の`overflow-y:auto`によるクリッピングを受けない。

**動作確認**: 実ブラウザで25プリセット全て×6テンプレートの組み合わせ（150通り）を`render()`しエラー無しを確認。パネルを開いて先頭グループが「おすすめ」（gothic/impact/brush）になっていること、フォント選択後に✓マークが正しく移動しボタンのプレビューテキスト・フォントが更新されること、`localStorage`の使用回数が加算され再度パネルを開くと「よく使うフォント」グループに反映されることを確認。パネル外クリック・Escapeキーでの閉じる動作を確認。スクリーンショットで、各行が実際にそのフォントで描画され、下に英字／和文の実書体名が表示されていることを視覚確認。

---

### ⑥キャラクターラインナップ型を追加（アンサンブル・回顧展系） (2026-09-03 追加実装14)

**背景**: 「他にあると便利な機能って何があるっけ？」という相談の中で、既存6テンプレートが全て「素材1点をどう見せるか」前提の設計になっていて、GAAATがよくやる「歴代シリーズを横断する」ような回顧展・アンサンブルもの（複数キャラクターが並ぶ構図）に対応できていない、という課題を洗い出し、「キャラクターラインナップ型の実装をお願い」という依頼で着手。

**実装**:
- `state.lineupImages`（`{image, name}`の配列、最大`LINEUP_MAX_IMAGES`=6枚）を新設。既存の`state.artImage`（単一画像、他の全テンプレートが使う）とは完全に独立した別スロットで、①〜⑤の挙動には一切影響しない。
- `index.html`の「素材選定」欄に、⑥選択時だけ表示される複数アップロード欄（`<input type="file" multiple>`）とサムネイルグリッド（順番バッジ・削除ボタン付き）を追加。ファイル選択はFileReader/Imageの読み込みが非同期で完了順が入れ替わり得るため、選択順を保った状態で配列にsplice挿入する処理にしている。
- `renderLineupTemplate()`を新設。タイトル・会場/会期の行・CTAピル・コピーライトなど全体構造は③ギャラリー型とほぼ同一（同じ関数を複製・改変）で、③の単一フレームパネル部分だけを`drawCharacterLineup()`（複数キャラクターの横並び描画）に差し替えている。
- キャラクター素材は`drawCoverImageTo`（クロップして敷き詰める）ではなく新設の`drawContainImageTo`（クロップせず全身を収める、下端揃え）で描画——ラインナップの目的は全身が見えることなので、既存テンプレートで使っている「はみ出た分をクロップ」方式だと構図によって頭や足が切れてしまうため、あえて別方式にした。各キャラクターは等幅スロットに割り当て、共通の接地線に下端を揃えて並ぶ。
- 個別キャラクターごとの調整（サイズ・位置）は非対応。既存の`art`調整カテゴリ（レイヤー分けZIP/PSD書き出しの「artwork」レイヤーも共用）をそのまま流用し、ドラッグ・拡大縮小・非表示は「グループ全体」に対して一括で効く——⑤つの既存テンプレートと同じ操作感を保ちつつ実装コストを抑えるトレードオフとして、意図的に個別調整は見送った。
- 配色抽出（背景色・アクセントカラー自動抽出）は`state.artImage`からのみ行われる既存仕様のままで、`lineupImages`からは行わない（複数画像のパレット合成は複雑になるため見送り）。⑥選択時に「素材選定」欄の単色アップロードで配色用の代表画像を別途アップロードするか、色ピッカーで手動調整する運用を想定し、その旨をヒント文で案内。
- `maybeAutoSelectTemplate()`に「現在⑥なら何もしない」ガードを追加——⑥は`classifyTemplateFromPalette()`の自動選択候補に含めていないが、ガード無しだと「自動選択」チェックがオンのまま⑥を選んで単一画像もアップロードした場合に、勝手に別テンプレートへ切り替わってしまうため。
- 書き出し前のバリデーション（`confirmProceedWithoutArt`/一括生成/一括プレビューの「素材がありません」チェック）は、これまで`state.artImage`の有無だけを見ていたのを、新設の`hasArtworkForExport()`（⑥の場合は`lineupImages.length > 0`、それ以外は従来通り`artImage`の有無）に一本化——⑥だけを使っていて`artImage`をアップロードしていないケースを誤って「素材なし」と警告しないようにした。

**動作確認**: 実ブラウザで全7テンプレート×2用途（14通り）のレンダリングエラー無しを確認。合成テスト画像3〜6枚での複数アップロード（順序保持・削除ボタンでの個別削除・6枚上限）、グループ全体の`scale`/`dx`/`dy`調整と`hidden`切り替え（実際に合成後キャンバスのピクセルで検証）、⑥選択時のみアップロード欄が表示されること、⑥から他テンプレートへの切り替えで欄が隠れることをプログラム的に確認。スクリーンショットで、タイトル・会場/会期行・複数キャラクター（下端揃え）・CTAピルが意図通りのレイアウトで並んでいることを視覚確認。
